# Batch 95 — Mypy override cleanup + fallback structured logging (audit P2 #7 combo)
Date: 2026-04-23
Tier: XS (2 файла, 0 live quota, quick win)

Context (CX read-first):
- Audit 2026-04-23 обнаружил два маленьких связанных P2 issue:
  1. (`docs/audits/2026-04-23-post-phase-2-audit.md:178-180`): `mypy --strict` reports `pyproject.toml: note: unused section(s): module = ['test_app_startup', 'test_orchestrate_timeout', 'test_postgres_live']`. Stale override block в `pyproject.toml:106-112` — модули не найдены (либо никогда не существовали, либо переименованы). Можно удалить.
  2. (`docs/audits/2026-04-23-post-phase-2-audit.md:185-188`): Fallback path в `src/gracekelly/core/router.py:325-365` (после batch-89/93 — теперь `self._settings.enable_model_fallback` вместо module-global) не эмитит structured log events. Adjacent failure paths logging (`routes/orchestrate.py:258-375`, `playwright_driver.py:257-422`) подробны, fallback — молчит. Operators реконструируют fallback только из `result.details` post-factum.
- Baseline: HEAD после batch-94. Пул уже через `.ready` self-chain (см. batch-94 closure-chain-next HARD RULES). CX подхватывает этот spec автоматически.
- 0 live Perplexity submits. Pure log addition + config cleanup.

Goal батча: чистый mypy вывод без "unused section" note; fallback transition теперь виден в логах (attempt / skip / success / failure) структурированно, похоже на existing logger calls в router.

---

## Task: CLEANUP-mypy-and-fallback-logging

goal: Удалён stale mypy override block; `_try_fallback` эмитит structured log events на attempt / skip / success / failure.
scope: `pyproject.toml`, `src/gracekelly/core/router.py` — 2 файла.
done_when:
- **pyproject.toml** (`[[tool.mypy.overrides]]` at line 106-112):
  - Весь block удалён (строки 106-112 вместе с header `[[tool.mypy.overrides]]`);
  - **НЕ** трогать adjacent `storage.postgres` override (lines 99-104) — он actually активен и нужен;
  - `.venv/Scripts/python.exe -m mypy src --strict` запуск → `Success: no issues found in <N> source files`, без `unused section(s)` note в stderr. Если note осталась — либо удалено не то, либо file parse issue, расследовать.
- **Router logging** (`src/gracekelly/core/router.py`, method `_try_fallback` at line ~325-365):
  - При входе, если условия fallback не выполняются — structured log `logger.debug("fallback skipped: <reason>", extra={"task_id": ..., "step_index": ..., "model_id": step.model.id, "skip_reason": <"disabled"|"code_not_triggering"|"no_fallback_id"|"fallback_model_not_found">})`;
  - Перед attempt (`fallback_step` построен, dispatch сейчас): `logger.info("fallback attempting: %s -> %s reason=%s", step.model.id, fallback_model.id, primary_result.failure_code.value, extra={"task_id": ..., "primary_failure_code": primary_result.failure_code.value, "fallback_to": fallback_model.id})`;
  - После completion fallback (success): `logger.info("fallback succeeded: %s -> %s", step.model.id, fallback_model.id, extra={...})`;
  - После completion fallback (failed — fallback adapter тоже вернул FAILED): `logger.warning("fallback failed: %s -> %s primary=%s fallback=%s", step.model.id, fallback_model.id, primary_result.failure_code.value, fallback_result.failure_code.value if fallback_result.failure_code else "unknown")`;
  - Log format matches existing `logger.info` calls в router (check style consistency — `logger = logging.getLogger(__name__)` уже есть в файле);
  - Existing logic `_try_fallback` **не** меняется — только добавляются log statements в существующие branches. No behaviour change.
- New test(s) в `tests/test_router_fallback.py` (минимум 1, можно 2):
  - `test_try_fallback_logs_attempt_and_success`: при successful fallback — caplog содержит "fallback attempting" и "fallback succeeded" сообщения с правильными model id;
  - Optional: `test_try_fallback_logs_skip_reason_when_disabled`: env fallback disabled → caplog содержит "fallback skipped" с reason="disabled";
  - Используй pytest `caplog` fixture для assertions на log records;
- R1 pass ≥ (baseline batch-94 + 1-2 новых тестов);
- `ruff check src tests` clean; `mypy src --strict` clean **без "unused section" note**;
- Existing fallback tests (batch-89) продолжают проходить unchanged.
- Zero изменений в: `main.py`, routes, UI, other tests.
visual_check: false
blocked_by: []

Notes:
- Log level choices (debug/info/warning) — руководствоваться существующими patterns в `router.py` и других adapters. Structured `extra={...}` — для machine parsing; log string — human readable. Consistency важнее чем оптимальный level.
- Если `ExecutionResult.failure_code` Optional и надо handle None case — это edge в warning branch; см. existing `_resolve_task_failure` для паттерна.
- Если CX считает что warning слишком громко для "fallback failed but primary already failed anyway" — оставить info. Judgement call.

---

## Task: closure-chain-next

goal: workflow artifacts, commits созданы CX; self-chain к batch-96.
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-95.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-95.md` есть, inbox не содержит `batch-95.md`;
- `.workflow/outbox/.done` JSON exit_code=0;
- commits созданы CX: `batch-95: CLEANUP-mypy-and-fallback-logging — <phrase>`, `batch-95: closure — refresh .done`;
- **Self-chain**: если в inbox остаётся `2026-04-23-batch-96.md` (или иной spec) — `.ready` остаётся/re-touch'ается. Если inbox пуст — `.ready` удалён;
- `git status --short` только `?? CLAUDE.md`.
visual_check: false
blocked_by: [CLEANUP-mypy-and-fallback-logging]

---

## HARD RULES

- **Scope tight**: 2 файла, оба уже tracked.
- **No behaviour change in `_try_fallback`**: только logging addition.
- **0 live submits**.
- **Self-chain `.ready`** если batch-96 ещё в inbox.
- **Commits создаёт CX**.
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: spec не будет переписан.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-95-*-report.md` + `.result.json`. Report: (a) mypy вывод before/after, (b) количество новых log statements, (c) test names.
