# Batch 96 — README API inventory + architecture.md sync (audit P2 #2 + #3)
Date: 2026-04-23
Tier: S (2 файла, 0 live quota, pure docs)

Context (CX read-first):
- Audit 2026-04-23 (`docs/audits/2026-04-23-post-phase-2-audit.md:222-233`):
  - README.md API-таблица (`README.md:81-96`) пропускает минимум 4 endpoint'а: `GET /api/v1/analytics` (`src/gracekelly/api/routes/analytics.py:31`), `POST /api/v1/orchestrate/upload` (`routes/orchestrate.py:393`), `POST /api/v1/tasks/{task_id}/retry` (`routes/orchestrate.py:742`), `POST /api/v1/models/refresh` (`routes/models.py:193`);
  - `docs/architecture.md` не упоминает post-Phase-2 modules: `core.budget`, `api.routes.analytics`, `api.routes.health_detailed`, `core.session_context`, `request_metrics`, `telemetry`. Drift — under-documentation, не stale references;
- Audit phrased decision: "If README is intended as product-level orientation, this is acceptable. If it is intended as endpoint inventory, it is stale." CX сам принимает решение: **README = full API inventory** (совпадает с текущим форматом таблицы) — дополнить всеми endpoint'ами, либо явный downgrade до product-level с ссылкой на архитектуру/OpenAPI.
- Baseline: HEAD после batch-95. Self-chain через `.ready`.
- 0 live Perplexity submits. Pure docs batch.
- CX последний в серии audit follow-up (93-96). После landing — "book closed, maintenance-only" mode.

Goal батча: README API-таблица = полный endpoint inventory текущего состояния; architecture.md упоминает все значимые post-Phase-2 модули.

---

## Task: DOCS-readme-api-inventory

goal: README API-таблица дополнена missing endpoints + actual state sync.
scope: `README.md` — 1 файл.
done_when:
- CX делает grep по `src/gracekelly/api/routes/` для **полного** списка зарегистрированных endpoint'ов (использовать `@router.get\|@router.post\|@router.put\|@router.delete` decorators pattern). Список фиксируется в report.
- README API-таблица (`README.md:81-96` или актуальное место) дополнена как минимум следующими endpoint'ами (которых audit не нашёл):
  - `GET /api/v1/analytics` — analytics endpoint;
  - `POST /api/v1/orchestrate/upload` — orchestrate с file attachment;
  - `POST /api/v1/tasks/{task_id}/retry` — task-level retry (Phase 4 AccountPool work);
  - `POST /api/v1/models/refresh` — runtime model catalog refresh;
  - Любые другие endpoint'ы которые grep нашёл, но в таблице отсутствуют (CX делает полную сверку, не только audit-cited);
- Table format matches existing style (columns: Method | Path | Description или аналог);
- Если CX видит stale/renamed endpoints в existing table (которых нет в коде) — **тоже fix** (удалить);
- README "Endpoints" или "API" section header неизменен (если есть);
- Environment variables table (если есть в README) — verify сверкой с `src/gracekelly/config.py`, fix явные mismatch'ы (audit уже зафиксировал `RATE_LIMIT_RPM` но batch-93 закрыл в roadmap — здесь double-check);
visual_check: false
blocked_by: []

---

## Task: DOCS-architecture-post-phase-2-sync

goal: `docs/architecture.md` упоминает current module surface (budget, analytics, health_detailed, session_context, request_metrics, telemetry, audits dir).
scope: `docs/architecture.md` — 1 файл.
done_when:
- Прочитать текущий `docs/architecture.md` целиком;
- Добавить секцию / bullet'ы / table entries для следующих модулей (если ещё нет):
  - `src/gracekelly/core/budget.py` (batch-90) — RequestBudgetTracker для browser quota;
  - `src/gracekelly/api/routes/analytics.py` — analytics endpoint;
  - `src/gracekelly/api/routes/health_detailed.py` — detailed health endpoint (Phase 11);
  - `src/gracekelly/core/session_context.py` — session/prompt shaping;
  - `src/gracekelly/request_metrics.py` (или где он реально) — RequestMetrics aggregation;
  - `src/gracekelly/telemetry.py` — observability setup;
  - Optional: `docs/audits/` — reference, что post-phase audits сохраняются тут;
- Если `docs/architecture.md` имеет layered структуру (adapter → core → api / layers section) — каждый новый module сажаем в правильный layer;
- Сохранить existing references (adapters.browser.scripted etc.) — они валидны;
- Verify: в архитектуре нет references на несуществующие модули (double-check post-cleanup);
- Overall document стиль consistent — CX не переписывает existing разделы, только дополняет / corrects.
visual_check: false
blocked_by: [DOCS-readme-api-inventory]

---

## Task: closure

goal: workflow artifacts, commits созданы CX. **Последний batch серии** — `.ready` **удалён** (не chain'им).
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-96.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-96.md` есть, inbox пуст (только Archive/);
- `.workflow/inbox/.ready` удалён окончательно;
- `.workflow/outbox/.done` JSON exit_code=0;
- commits созданы CX: `batch-96: DOCS-readme-api-inventory — <phrase>`, `batch-96: DOCS-architecture-post-phase-2-sync — <phrase>`, `batch-96: closure — refresh .done`;
- `git status --short` только `?? CLAUDE.md`;
- `git add -f` для workflow artifacts.
visual_check: false
blocked_by: [DOCS-architecture-post-phase-2-sync]

---

## HARD RULES

- **Scope tight**: 2 файла. `README.md` + `docs/architecture.md`. **Zero** в `src/`, tests, other docs, pyproject.
- **0 live submits**.
- **Full inventory pass**: CX делает grep по routes/ для полного endpoint списка, не только audit-cited 4 endpoint'а.
- **No existing section rewrites**: дополняем, не переписываем.
- **Ready удалён окончательно** — это последний batch серии 93-96.
- **Commits создаёт CX**.
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: spec не будет переписан.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-96-*-report.md` + `.result.json`. Reports: (a) endpoint count before/after в README, (b) list of added module references в architecture.md, (c) closure — подтверждение что inbox пуст и batch-серия 93-96 закрыта.
