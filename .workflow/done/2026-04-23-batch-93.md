# Batch 93 — Settings injection + roadmap sync (audit P1 #1 + P1 #3)
Date: 2026-04-23
Tier: S (router + main + tests + docs — 4 файла; 0 live quota)

Context (CX read-first):
- Audit 2026-04-23 (`docs/audits/2026-04-23-post-phase-2-audit.md:54-57`) зафиксировал **реальный config bug**: `create_app(Settings(...))` строит `ExecutionRouter` без передачи settings (`src/gracekelly/main.py:400-405`), а router читает budget/fallback из module-global `_default_settings` (`src/gracekelly/core/router.py:8, 46-47, 331-346`). Результат: `app.state.settings.enable_model_fallback == True` но `app.state.execution_router.healthcheck()["budget"]` возвращает `{'per_task_limit': None, ...}`. Существующие тесты патчат module-global (`tests/test_request_budget.py:349-378`, `tests/test_router_fallback.py:135-499`) — это workaround хіддена.
- Audit также зафиксировал docs drift в `docs/phased-roadmap.md`:
  - `Phase 2 Status: partial` (line 39) — на самом деле закрыто в batch-91 (harness extended на 5 patterns);
  - `Phase 4 Status: partial` (line 93) — закрыто в batch-89/90 (fallback + budget);
  - `GRACEKELLY_RATE_LIMIT_PER_MINUTE` (line 112) — код использует `GRACEKELLY_RATE_LIMIT_RPM` (`src/gracekelly/config.py:202`);
  - `✅ CORS support` (line 203) — grep не находит `CORSMiddleware`/`allow_origins` в `src/` — claim ложен.
- Baseline: HEAD `3aff7eb`, 2647 passed / 6 skipped / 97% coverage. ruff + mypy --strict clean.
- 0 live Perplexity submits. Unit-testable.
- Design: `ExecutionRouter.__init__` принимает `settings: Settings | None = None`. Если None — падает на `_default_settings` (backward-compat для tests и direct instantiation). В `create_app` main.py явно передаёт `settings=active_settings`. Router внутри читает через `self._settings.enable_model_fallback` etc.
- Existing tests должны продолжить работать: те что патчат `_default_settings` — получат update на передачу custom `Settings` в `ExecutionRouter(...)` constructor (более явный DI).

Goal батча: (1) `create_app(Settings(...))` реально управляет budget/fallback behavior; (2) `docs/phased-roadmap.md` отражает текущее состояние; roadmap Phase 2/4 переведены в Delivered, rate-limit env name синхронизирован с кодом, CORS claim удалён.

---

## Task: CORE-inject-settings-into-router

goal: `ExecutionRouter` принимает settings через constructor; `create_app` передаёт `active_settings`; module-global `_default_settings` остаётся fallback для prod BC, но production path его не использует.
scope: `src/gracekelly/core/router.py`, `src/gracekelly/main.py`, `tests/test_router_fallback.py`, `tests/test_request_budget.py` — 4 файла.
done_when:
- **Router extension** (`src/gracekelly/core/router.py`):
  - `ExecutionRouter.__init__` добавляет параметр `settings: Settings | None = None` (import из `gracekelly.config` — найти актуальное class name через Grep, скорее всего `Settings` или аналог);
  - если `settings is None` → `self._settings = _default_settings` (backward-compat для прямого instantiation и тестов, которые ещё не обновлены);
  - иначе → `self._settings = settings`;
  - Замены внутри методов:
    - line 46-47 `_default_settings.max_browser_submits_per_task/_per_hour` → `self._settings.max_browser_submits_per_task/_per_hour`;
    - line 106-107 `getattr(_default_settings, ...)` → `getattr(self._settings, ...)`;
    - line 331 `_default_settings.enable_model_fallback` → `self._settings.enable_model_fallback`;
  - `_default_settings` import сохраняется (как default-fallback), но ремарка в комментарии 1 строкой: `# _default_settings kept as a fallback for direct instantiation; production path injects via create_app.`;
- **App factory** (`src/gracekelly/main.py:400`):
  - `ExecutionRouter(... , settings=active_settings)` явно передано;
  - никаких других изменений в `main.py` (не трогать create_app signature, lifespan, etc).
- **Tests update**:
  - `tests/test_request_budget.py:349-378` — тесты которые сейчас патчат `gracekelly.core.router._default_settings` для budget: переписать на передачу `settings=Settings(max_browser_submits_per_task=N, max_browser_submits_per_hour=M)` в ExecutionRouter constructor (или соответствующий mock Settings object). Patch-based стиль можно оставить как fallback если кажется более читаемым — но хотя бы 2 теста в каждом файле переписать на explicit injection;
  - `tests/test_router_fallback.py:135-499` — аналогично для `enable_model_fallback`;
  - Обязательные новые тесты:
    - (new-1) `test_router_uses_injected_settings_for_budget`: `ExecutionRouter(..., settings=Settings(max_browser_submits_per_task=2))` → после 2 acquires third returns RATE_LIMITED — injection работает;
    - (new-2) `test_router_uses_injected_settings_for_fallback`: `settings=Settings(enable_model_fallback=True)` + primary AUTH_FAILED → fallback triggered (без module-global patch);
    - (new-3) `test_create_app_propagates_settings_to_router`: `app = create_app(Settings(max_browser_submits_per_task=3, enable_model_fallback=True))` → `app.state.execution_router.healthcheck()["budget"]["per_task_limit"] == 3`. Этот test **reproduces the audit bug** — он должен падать до фикса в main.py и проходить после.
  - R1 pass ≥ 2647 + 3 = 2650 (3 новых теста); existing tests зелёные (в т.ч. если CX переписал несколько patch-based на explicit — они продолжают подтверждать ту же семантику).
  - `ruff check src tests` clean; `.venv/Scripts/python.exe -m mypy src --strict` clean.
- **Zero изменений** в: `src/gracekelly/config.py` (settings class не трогается — просто читаем), routes, UI, adapters, `orchestrator.py`.
visual_check: false
blocked_by: []

Notes:
- Если `Settings` class не имеет всех полей с defaults (т.е. нельзя сделать `Settings(max_browser_submits_per_task=3)` без других required fields) — CX либо (a) использует `dataclasses.replace(_default_settings, max_browser_submits_per_task=3)` в тестах, (b) создаёт маленький `Mock` или `SimpleNamespace` с нужными атрибутами. Выбрать то что дает cleanest тест.
- `OrchestratorService` уже принимает `settings=active_settings` (`main.py:407-410`) — следуем тому же паттерну.
- `_default_settings` rename — НЕ делать. Keep as-is (`from gracekelly.config import settings as _default_settings`), только add usage переход к `self._settings`.

---

## Task: DOCS-roadmap-sync

goal: `docs/phased-roadmap.md` отражает реальное состояние после batch-91, env name корректен, CORS claim удалён.
scope: `docs/phased-roadmap.md`
done_when:
- **Phase 2** (line ~37-68): Status changed from `partial, first authenticated live-driver smoke proven` на `complete` (или `complete, live-smoke harness extended to smart/debate/consensus/compare/upload in batch-91`). Добавить новую строку в Delivered:
  - `Delivered after Phase 2 closing:` section (новая подсекция или inline): `- **batch-91 HARNESS-expand-patterns**: live smoke harness covers smart/debate/consensus/compare/upload с pattern-specific evaluation; operator runbook section added (\`a76b632\`, \`b965c3c\`)`.
- **Phase 4** (line ~91-117): Status changed from `partial` на `complete`. Добавить в Delivered:
  - `- **batch-89 CORE-model-fallback-policy**: ModelSpec.fallback_model_id + single-shot ExecutionRouter fallback на AUTH_FAILED/PROVIDER_UNAVAILABLE/TIMEOUT; env GRACEKELLY_ENABLE_MODEL_FALLBACK default off (\`8665875\`)`
  - `- **batch-90 CORE-request-budget-browser**: RequestBudgetTracker with per-task + rolling-hourly caps; browser-only gate; env GRACEKELLY_MAX_BROWSER_SUBMITS_PER_TASK / _PER_HOUR default None (\`bd8d978\`)`
  - `- **batch-93 CORE-inject-settings-into-router**: ExecutionRouter принимает explicit Settings через create_app, устранён module-global config leak (\`<SHA>\`)`
- **Rate-limit env rename** (line 112): `GRACEKELLY_RATE_LIMIT_PER_MINUTE` → `GRACEKELLY_RATE_LIMIT_RPM` (это единственное место в роадмапе — grep verify);
- **CORS claim removal** (line 203): строка `- ✅ CORS support (configurable origins, credentials, CORS middleware)` удалена **полностью** (не через strike-through). CORS не реализована и не нужна для single-user local. Если после удаления остаётся пустой list item — удалить и его.
- **Last-updated header** (line 3): обновить до `Last updated: 2026-04-23 (Phase 2 + Phase 4 closed via batch-89/90/91/93; audit P1 roadmap sync applied)` или аналог.
- Phase 17 Remaining пункты (cyrillic, AUTH3) **нетронуты** — они не в этом батче;
- `docs/operator-runbook.md` не трогаем (audit Section 8.4: "largely synced");
- `README.md` не трогаем (отдельный batch-96);
- `docs/architecture.md` не трогаем (отдельный batch-96);
- Commit: `batch-93: DOCS-roadmap-sync — Phase 2/4 closed, env/CORS corrections applied`.
visual_check: false
blocked_by: [CORE-inject-settings-into-router]

---

## Task: closure

goal: workflow artifacts, commits созданы CX.
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-93.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-93.md` есть, inbox пуст, `.ready` удалён, `.done` JSON exit_code=0;
- commits созданы CX: `batch-93: CORE-inject-settings-into-router — <phrase>`, `batch-93: DOCS-roadmap-sync — <phrase>`, `batch-93: closure — refresh .done`;
- `git status --short` только `?? CLAUDE.md`;
- `git add -f` для workflow artifacts.
visual_check: false
blocked_by: [DOCS-roadmap-sync]

---

## HARD RULES

- **Scope tight: 4 prod файла + 1 docs**. Router + main + 2 tests + roadmap.md. Если CX видит что нужно тронуть `config.py` или `orchestrator.py` — STOP + diagnostic.
- **Backward-compat**: `ExecutionRouter(dry_run_adapter=...)` без `settings=` всё ещё работает через `_default_settings` fallback (для прямого instantiation и старых tests, которые ещё не переведены — переводим минимально, основная масса может остаться patch-based).
- **Reproducer test (new-3) должен падать до фикса `main.py:400`**: CX запускает тест до изменения main.py чтобы подтвердить — баг реально воспроизводится (это audit finding). После фикса — зелёный. Отметить в report.
- **CORS claim полностью удалён** (не strike-through, не renamed). Это "либо документируем либо реализуем" — документация соврала, фиксим через удаление.
- **0 live Perplexity submits**. Unit-only.
- **Commits создаёт CX** (3 atomic).
- mypy через `.venv/Scripts/python.exe -m mypy src --strict`. ruff clean.
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: spec не будет переписан.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-93-CORE-inject-settings-into-router-report.md` + `.result.json`;
`.workflow/outbox/2026-04-23-batch-93-DOCS-roadmap-sync-report.md` + `.result.json`;
closure report со SHA всех commits. CORE report должен явно зафиксировать: (a) что reproducer test (new-3) падал до main.py фикса (audit bug confirmed), (b) сколько tests переписаны на explicit injection vs оставлены patch-based, (c) healthcheck()["budget"] результат в тесте.
