# Batch 92 — Post-Phase-2 comprehensive audit (read-only, no fixes)
Date: 2026-04-23
Tier: M (single deliverable — one audit report; 0 production changes; 0 live quota)

Context (CX read-first):
- Phase 2, 4, 17 все закрыты. Roadmap ~95% complete. HEAD `7e50fb7`, 2647 passed / 6 skipped / 97% coverage. Кумулятивно от batch-69 до batch-91 — 3 недели плотной работы, ~25 batches, ~20k lines добавлено/изменено.
- **Goal аудита**: выявить accumulated tech debt, architectural drift, tooling/deps gaps, docs/code desync, prioritized findings для post-closing fix-батчей. **Никаких изменений кода**. Только observation + recommendations.
- Roadmap state как baseline — `docs/phased-roadmap.md`. Open items: Phase 1 Gate 2 + Gate 3 (operational + execution-policy reviews), Phase 17 Remaining cyrillic (not our zone) + AUTH3 (deferred), Parallel track "Next" (API hedge expansion, low-priority).
- **Не трогать** в этом батче: production code (`src/`), tests, scripts, конфиги. Единственный output — `docs/audits/2026-04-23-post-phase-2-audit.md` + `.workflow/outbox/` reports.
- Available tooling: `.venv/Scripts/python.exe -m ruff check ...`, `.venv/Scripts/python.exe -m mypy src --strict` (strict уже enabled в pyproject.toml), `.venv/Scripts/python.exe -m coverage report --show-missing --sort=cover`, `.venv/Scripts/python.exe -m pytest --durations=20`, `.venv/Scripts/python.exe -m pip list --outdated`. Дополнительные tools (pip-audit, radon, vulture) — CX может установить в venv через `.venv/Scripts/python.exe -m pip install <tool>` если считает нужным, но установка **не** обязательна; если alternative path через ruff/grep достаточен — предпочтительнее.
- Pre-read для context: `docs/architecture.md`, `docs/operator-runbook.md`, `docs/phased-roadmap.md` последние 50 строк (Phase 15+).

Goal батча: один comprehensive audit report `docs/audits/2026-04-23-post-phase-2-audit.md` с executive summary + findings по 6 осям (architecture / security / dependencies / test robustness / code quality / docs sync), приоритизированными P0/P1/P2/P3. CX сам фиксирует health score и рекомендации follow-up батчей. CC (я) потом приоритизирует и выберет что делать.

---

## Task: AUDIT-post-phase-2

goal: Структурированный read-only audit report, покрывающий 6 осей, с actionable findings + priority.
scope: `docs/audits/2026-04-23-post-phase-2-audit.md` (NEW, директория создаётся), `.workflow/outbox/2026-04-23-batch-92-AUDIT-report.md` (execution summary).
done_when:
- Директория `docs/audits/` создана (если её ещё нет), отчёт сохранён там;
- Report содержит **обязательные** разделы (в этом порядке):

  1. **Executive Summary** (1 параграф ~10 строк):
     - Health score 0-10 с обоснованием;
     - Top-3 приоритетных findings (одна строка каждый: `P0: <one-line>`);
     - Общий verdict: "ready for long-term maintenance" / "needs targeted hardening" / "significant accumulated debt";

  2. **Baseline metrics snapshot** (одна небольшая таблица):
     - source files (count), source LOC;
     - test files (count), test LOC;
     - coverage total + top-5 least-covered modules (% + module name);
     - mypy --strict result (pass/fail + error count если any);
     - ruff total violations (если any — по default config из pyproject);
     - pytest duration total + top-5 longest tests;

  3. **Architecture findings**:
     - Layering: core ↔ adapters ↔ api границы соблюдены? (CX проверяет через grep imports по правилу: api НЕ импортирует adapters напрямую, adapters НЕ импортирует api). Если нарушения — list.
     - Cyclomatic / size outliers: файлы >600 LOC, функции >100 LOC — list (file:line + count).
     - DI patterns consistency: есть ли разные стили (constructor vs factory vs singleton) для одной роли? Cite 2-3 example'а если есть.
     - Circular imports: ruff или mypy уже обычно ловят — CX verify no issue, или list if any.

  4. **Security findings** (повторно post Phase 13 audit — что изменилось):
     - `pip-audit` (если установлен) — CVE по зависимостям; иначе skip с пометкой "tool not installed, no new vulnerabilities known since Phase 13 сверка `docs/phased-roadmap.md:200`";
     - Grep on high-signal patterns: `os.system\|subprocess.call\|eval\(\|pickle.loads\|yaml.load\(` — list с file:line contextом (false-positives OK, пометить);
     - Secrets / env leakage: grep `password\|api_key\|secret` в `src/` (не включая config.py где они легитимны); любое hard-coded значение без env-lookup — flag;
     - Path traversal: `open(\|Path(` с user-controlled input (adapter, routes) — CX sanity-check что все валидируются;
     - CORS / auth opt-in: `GRACEKELLY_API_KEY` / `GRACEKELLY_RATE_LIMIT_PER_MINUTE` — default значения безопасны?

  5. **Dependency health**:
     - `pip list --outdated` output (subset из main deps, не dev) — major-version drift flag'ать;
     - Unused deps: декларированные в `pyproject.toml` но не импортируемые в `src/` — grep-based check;
     - Heavy deps assessment: что самое тяжёлое, используется ли полноценно или только 1-2 api'шки;

  6. **Test robustness**:
     - Coverage gaps: модули с <85% — list;
     - Known flaky history: grep по `.workflow/outbox/*.md` на `flaky\|flake\|stabiliz` за последние 30 дней, инвентаризация какие тесты упоминаются;
     - Duration outliers: `pytest --durations=20` — list, оценить "ожидаемо slow" (integration/live) vs "should be fast but isn't";
     - Mutation-test coverage note: batch-CX R5 battery проверяет mutation kill — но это ad-hoc per batch, не systemic. Рекомендация: запускать `mutmut` / `cosmic-ray` на hot modules? (не imperative, предложение);
     - Test parity: для `core/budget.py` (batch-90), `core/router.py._try_fallback` (batch-89), `adapters/browser/playwright_driver.py._find_option_in_menu_scope` (batch-87) — coverage > 95%?;

  7. **Code quality**:
     - Dead code: `coverage report` показывает 0%-модули; grep на unused public symbols — list (false-positives OK);
     - Bare `except:` — grep `except:\s*$\|except:\s*pass` в `src/` — list;
     - Logging gaps в new code (batch-86/89/90/91): есть ли structured log во всех failure branches?;
     - Docstring coverage: public classes/functions в `src/gracekelly/core/` без docstrings — список (top 10);
     - Type hints: `Any` abuse в hot paths — grep `: Any` / `-> Any` и оценить легитимность;

  8. **Docs sync**:
     - `README.md` vs код: все ли environment variables задокументированы? Все ли endpoints покрыты? (не обязательно — product docs vs API reference, отметь какой уровень);
     - `docs/operator-runbook.md` vs код: есть ли команды/endpoint'ы, которые reference'ят что-то с переименованием? Harness quota estimates из batch-91 соответствуют наблюдаемым из Phase 17 real runs?;
     - `docs/architecture.md` vs фактическая структура: модули, которые упоминаются, существуют? новые модули (budget.py, scripted.py etc) упомянуты?;
     - `docs/phased-roadmap.md` — Phase 2/4 Status полей точны? Если "partial" где уже closed — flag как docs drift (CX может обновить тогда же, но тут это НЕ в этом batch, только flag);
     - docstrings vs signature: spot-check 5-10 public функций — описание параметров соответствует signature?;

  9. **Recommendations** (numbered list):
     - Proposed follow-up batches: каждый с priority (P0/P1/P2/P3), size estimate (XS/S/M/L), scope hint (файлы), 1-строчным rationale;
     - Explicitly **not to fix**: что обнаружено, но deprioritized (и почему — ROI argument);
     - If any finding рекомендует live validation — только manual, не scheduled;

  10. **Methodology footnote** (короткий):
      - Какие tools использованы, какие параметры, какой version;
      - Что **не** проверялось (limitations): performance profiling, load testing, multi-user scenarios (single-user local deploy);

- CX сам принимает P0/P1/P2/P3 priority на основе impact × effort:
  - **P0** — security/correctness bug, blocker для trusted use;
  - **P1** — обязательно сделать в ближайшие 2-3 batches (docs drift, coverage gap, deps outdated major-version);
  - **P2** — хорошо бы сделать when convenient (code quality, minor refactor);
  - **P3** — nice-to-have, можно жить без этого годами;
- Report targeted **для CC** (оркестратора), не для end-user: техничный тон, конкретные file:line ссылки, no fluff.
- **No issue inflation**: если axis чистая — одно предложение "no findings in this axis" достаточно. Не искусственно раздувать findings.
- **R1 pass** не меняется (zero code changes), но CX прогоняет `pytest -q` для baseline snapshot и включает результат в Section 2.
- `ruff check src tests` clean подтверждается. Если внезапно не clean (где-то drift'нуло) — это само по себе P1 finding.
- `mypy src --strict` clean подтверждается.
- `docs/audits/2026-04-23-post-phase-2-audit.md` exists, git-tracked.
- `docs/audits/` в .gitignore? — CX проверяет; если да — исключить через `!docs/audits/` или commit через `git add -f`. Предпочтительно: `docs/` целиком tracked, так что audit должен добавляться без force.
- Commit: `batch-92: AUDIT-post-phase-2 — health score <N>/10, <K> findings (<P0>/P0, <P1>/P1)`.
visual_check: false
blocked_by: []

Notes:
- Tools install (pip-audit и др.) — в venv `.venv/Scripts/python.exe -m pip install <tool>`, commit requirements не обновлять (audit tools — dev-only). Если CX установил — note в Section 10 methodology.
- Grep-based findings OK с false-positives; CX pометку "3 matches, 2 false-positive (legitimate logger.error), 1 real" в findings.
- Размер report ожидается 200-400 строк markdown. Если значительно больше — фильтровать только actionable findings, не dumping grep output.
- Если какая-то ось требует deeper dive, для которого нет времени (например, full dep graph analysis) — note в findings "partial analysis, full analysis recommended in future audit" + включить то что успел.

---

## Task: closure

goal: `.workflow/outbox/.done` exit_code=0, spec в `.workflow/done/`, `.ready` удалён. Коммиты созданы CX (batch-90 precedent).
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-92.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-92.md` есть, inbox пуст;
- `.workflow/inbox/.ready` удалён;
- `.workflow/outbox/.done` JSON exit_code=0;
- **Все commits созданы CX через `git commit`**: `batch-92: AUDIT-post-phase-2 — <phrase>`, `batch-92: closure — refresh .done`;
- `git status --short` возвращает только `?? CLAUDE.md`;
- `git add` для `docs/audits/2026-04-23-post-phase-2-audit.md` (если audit — в gitignore free зоне, без `-f`); `-f` для `.workflow/done/`, `.workflow/outbox/.done`, outbox reports.
visual_check: false
blocked_by: [AUDIT-post-phase-2]

---

## HARD RULES

- **ZERO изменений кода / тестов / scripts / configs.** Только создание `docs/audits/2026-04-23-post-phase-2-audit.md` + standard workflow artifacts в `.workflow/outbox/`. Если CX видит bug настолько серьёзный что хочется починить сразу — запрет; finding записывается в report P0, fix — отдельный batch-93+.
- **0 live Perplexity submits.** Audit — offline analysis.
- **Baseline sanity**: перед audit — прогнать `pytest -q` один раз, подтвердить 2647 passed / 6 skipped (precise numbers, не приближённые). Если расхождение — это P0 "test suite drift" finding.
- **mypy + ruff clean gates** подтверждены (если не clean — P1 finding).
- **Commits обязательно созданы CX** (batch-90 не создавал; с тех пор batch-91 создал — придерживаемся этого).
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`, installing deps globally (только в venv).
- Spec stability: spec не будет переписан во время выполнения.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-92-AUDIT-report.md` — execution summary (тот же report структурно, либо короткий wrapper "see docs/audits/..."); `.workflow/outbox/2026-04-23-batch-92-AUDIT.result.json` (Auto-Accept); closure report SHA всех коммитов.
