# Batch 97 — Final closure: Gate 2/3 self-review, AUTH3/cyrillic/API-hedge docs resolution
Date: 2026-04-23
Tier: S-M (4 docs файла, 0 live quota, 0 production code)

Context (CX read-first):
- После batch-96 у GraceKelly остались формально open только deferred items из Phase 1 и Phase 17:
  - **Phase 1 Gate 2** (operational review) + **Gate 3** (execution-policy review) — documented в roadmap как "not blocking Phase 1 completion, required before production hardening";
  - **Phase 17 Remaining cyrillic** — PowerShell harness encoding drop, **не наш код** (automation side);
  - **Phase 17 Remaining AUTH3** — `.workflow/outbox/2026-04-21-AUTH-AUTH3-skipped.md:1`: "rationale=low friction; AUTH1 sync mapping + AUTH2 inline banner/Retry already surface the Perplexity sign-in problem clearly, and no additional browser-session persistence was required for this batch." То есть уже **closed by design** — Chrome persistent profile + sign-in banner покрывают 100% single-user local use case;
  - **Parallel track "Next"**: API hedge beyond single OpenAI-compatible — reactive trigger ("if browser fragility proves material"), not an open deliverable.
- Baseline: HEAD `beb8a0e`, 2653 passed / 6 skipped / coverage 97%, ruff + mypy --strict clean. `docs/audits/2026-04-23-post-phase-2-audit.md` health 7.6/10 baseline.
- Для single-user local deploy **Gate 2/3 не могут быть closed external review** (внешнего reviewer нет). Standard practice для single-developer/small-scope проектов — self-review attestation with structured checklist. Это валидный closure. Scope это акт.
- 0 live quota. Pure docs.

Goal батча: все Phase 1 + Phase 17 open items формально закрыты through appropriate mechanism — self-review docs для gates, close-by-design аннотация для AUTH3, known-limitation note для cyrillic, trigger-reactive статус для API hedge. После landing — `docs/phased-roadmap.md` не содержит Remaining/Open items (кроме explicit trigger-reactive). GraceKelly формально в "complete roadmap" state.

---

## Task: GATES-self-review

goal: Создать `docs/gates/` директорию с self-review attestation для Gate 2 (operational) и Gate 3 (execution-policy), со структурированным checklist + pass/fail per criterion + signature.
scope: `docs/gates/2026-04-23-gate-2-operational-review.md` (NEW), `docs/gates/2026-04-23-gate-3-execution-policy-review.md` (NEW) — 2 файла.
done_when:
- Директория `docs/gates/` создана;
- **`docs/gates/2026-04-23-gate-2-operational-review.md`** содержит разделы:
  - **Header**: "Gate 2 — Operational readiness self-review", date `2026-04-23`, reviewer `CC/CX internal self-review (single-user local deploy scope)`, HEAD `beb8a0e`;
  - **Scope of review**: explain что `docs/phased-roadmap.md:34` says this gate was open ("operational review for readiness semantics"). Target: verify readiness semantics, health/readiness endpoints, graceful shutdown, logging, error surfacing адекватны для single-user local deployment;
  - **Criteria + verification** (checklist format, per item: criterion + how verified + pass/fail + evidence file:line):
    1. `/healthz/live` endpoint returns HTTP 200 with no external dependencies → verify via code `src/gracekelly/api/routes/health.py` (grep live endpoint) + test `tests/test_http_api.py` (grep relevant test) → PASS with evidence;
    2. `/healthz/ready` endpoint gates on browser adapter + storage readiness → verify same way → PASS;
    3. `/metrics` endpoint exposes runtime state and doesn't break on degraded components → PASS (batch-80 test visible in duration outliers);
    4. Lifespan hooks: startup initialises browser/storage, shutdown flushes/closes → verify `src/gracekelly/main.py` lifespan section → PASS;
    5. Structured logging: INFO on major state transitions (task start/complete, browser auth, fallback) — verify `logger.info` density + batch-95 fallback logs → PASS;
    6. Error surfacing: sync returns `HTTP 503 {"code":"model_auth_required"}` (AUTH1), async captures same in task.error (AUTH2 inline banner), budget/fallback details in response → verify route code → PASS;
    7. Observability for operators: operator-runbook current, health-endpoint discoverable, Prometheus metrics present — verify `docs/operator-runbook.md` exists and covers these → PASS;
  - **Findings / deviations**: (если все pass → "No deviations identified"; иначе список);
  - **Verdict**: `PASS for single-user local deployment scope`;
  - **Limitations of this review**: "Not covered: multi-user concurrency, cluster deployment, external SLA contracts, disaster recovery drills — these require a deployment context beyond single-user local";
  - **Signature**: `Reviewed by CC (Claude Code orchestrator) on 2026-04-23, commit pending`;
- **`docs/gates/2026-04-23-gate-3-execution-policy-review.md`** содержит разделы:
  - **Header**: "Gate 3 — Execution policy self-review", same date/reviewer/HEAD;
  - **Scope**: `docs/phased-roadmap.md:35` says this gate was open ("execution-policy review for defaults and failure handling"). Target: verify per-model timeouts, concurrency limits, retry policy, fallback chain, budget, failure taxonomy;
  - **Criteria + verification**:
    1. Per-model timeouts defined in `ModelSpec.timeout_seconds` для всех моделей (browser + API) → verify `src/gracekelly/core/models.py` → PASS;
    2. `ModelConcurrencyGate` + per-model concurrency_limit enforced in router → verify `core/router.py._execute_with_concurrency_limit` → PASS;
    3. Model fallback policy (batch-89): single-shot, explicit failure codes {AUTH_FAILED, PROVIDER_UNAVAILABLE, TIMEOUT}, env-gated default off → verify `core/router.py._try_fallback` + `settings.enable_model_fallback` → PASS;
    4. Request budget (batch-90): browser-only, per-task + rolling-hourly, env-gated default unlimited → verify `core/budget.py` + `core/router.py` + `config.py` env keys → PASS;
    5. Budget RATE_LIMITED **не** triggers fallback (intentional) → batch-89/90 test parity → PASS (test id'ы из test_request_budget.py);
    6. Task-level retry via `retry_of_task_id` + `/api/v1/tasks/{task_id}/retry` → verify `routes/orchestrate.py` endpoint → PASS;
    7. Settings injection flow (batch-93): `create_app(Settings(...))` propagates to `ExecutionRouter` → verify `main.py:400` + `router.py:ExecutionRouter.__init__` → PASS;
    8. Failure taxonomy (`FailureCode` enum) complete: AUTH_FAILED, MODEL_MISMATCH, PROVIDER_UNAVAILABLE, TIMEOUT, RATE_LIMITED, STORAGE_FAILED, UNKNOWN_ERROR → covers browser + API + infra modes → PASS;
    9. Adapter saturation visibility in healthcheck → `ExecutionRouter.healthcheck()` returns `budget` + `active_by_model` → PASS;
  - **Findings / deviations**: (same format);
  - **Verdict**: `PASS for single-user local deployment scope`;
  - **Limitations**: "Not covered: adversarial load testing, production-grade SLO tracking, multi-tenant quota fairness — out of scope";
  - **Signature**: same format;
- Оба файла — 60-120 строк каждый, clear structure, concrete file:line references (CX делает grep verify, не просто claim);
- `ruff`, `mypy`, `pytest` не затронуты (docs-only).
visual_check: false
blocked_by: []

Notes:
- CX **реально проверяет** каждый criterion через grep/Read, не просто claim "PASS". Evidence column должна быть actionable (file:line + тест id).
- Если **любой** criterion не проходит — это P0 finding для отдельного batch-98 + Gate остаётся "PASS with conditions" / "FAIL". Честный self-review — обязательно.
- No commit yet — batching с другими tasks в этом батче.

---

## Task: DOCS-final-roadmap-closure

goal: `docs/phased-roadmap.md` обновлён: Phase 17 Remaining пустой, deferred items перенесены в соответствующие "Closed by design" / "Deferred by design" subsections, Phase 1 gates marked pass (self-review), API hedge expansion помечен trigger-reactive. Plus cyrillic harness limitation note в `docs/operator-runbook.md`.
scope: `docs/phased-roadmap.md`, `docs/operator-runbook.md` — 2 файла.
done_when:
- **`docs/phased-roadmap.md`**:
  - **Header** (line 3): `Last updated: 2026-04-23 (full roadmap closure — all phases complete; gates self-reviewed; deferred items explicitly scoped)`;
  - **Phase 1 Open review gates** (line ~33-35): изменить секцию — добавить `Self-review outcome (2026-04-23):` subsection:
    - `- Gate 2 (operational readiness): PASS for single-user local scope — see \`docs/gates/2026-04-23-gate-2-operational-review.md\``;
    - `- Gate 3 (execution-policy): PASS for single-user local scope — see \`docs/gates/2026-04-23-gate-3-execution-policy-review.md\``;
    - Original "Open review gates" header change to `Review gates (closed via self-review 2026-04-23):`;
  - **Phase 17 Remaining** (line ~335-337): переведена в `Closed / Scoped out (2026-04-23):` subsection:
    - `- **Cyrillic prompts via some harnesses lose encoding** — closed as out-of-scope: lives on the PowerShell/automation-harness side, not in GraceKelly. See \`docs/operator-runbook.md\` "Harness limitations" note for operator guidance.`;
    - `- **AUTH3 persistent session reuse** — closed by design via batch-69 rationale (AUTH1 sync mapping + AUTH2 inline banner + Chrome persistent profile directory already cover single-user recovery). No separate implementation needed; batch-72 bootstrap covered profile lifecycle.`;
  - **Parallel track: API adapters → Next** (line ~155-156): переписать как:
    - Header changed from `Next:` на `Trigger-reactive follow-up (not scheduled):`
    - `- Expand API hedge beyond a single OpenAI-compatible model — activated only if browser fragility becomes material in production-like loads. Not a currently open item for single-user local deploy.`;
  - Ничего не удаляем — только reframe + closure annotations;
  - Phase 2-16 Delivered sections неизменны (batch-93 и batch-96 уже их sync'ли);
- **`docs/operator-runbook.md`** — добавить новую секцию "## Harness limitations" (или аналог, CX решает точное место — ближе к "Live smoke harness" из batch-91):
  - Подраздел "Cyrillic prompts via PowerShell pipe":
    - "When running the harness or any CLI tool that passes a cyrillic prompt through a PowerShell pipe (`echo 'привет' | python ...`), PowerShell's default encoding downgrades cyrillic to `?`-placeholders before the process sees the string. This is a PowerShell issue, not a GraceKelly bug.";
    - "**Workaround**: use `--prompt` argument directly (`python scripts/live_smart_smoke.py --prompt \"привет\"`), or set `$OutputEncoding = [System.Text.Encoding]::UTF8` in the session before piping, or use `--ascii-fallback` flag for deterministic ASCII prompts.";
    - Reference: batch-82 live SMART failure (documented in phased-roadmap Phase 17 Incidents);
  - Подраздел "Persistent session reuse" (nice-to-have 2-3 строки):
    - "Authentication is persisted automatically via the dedicated Chrome profile directory (default `chrome-profile/`, configurable via `GRACEKELLY_BROWSER_PROFILE_DIR`). There is no separate session token file. If the profile is busy (another Chrome process holds it), the adapter returns a profile-in-use error — see `docs/onboarding.md` for the bootstrap helper.";
- Changes — additive, не переписываем existing разделы;
- Commit: `batch-97: DOCS-final-roadmap-closure — close remaining deferred items, add harness limitations note`.
visual_check: false
blocked_by: [GATES-self-review]

---

## Task: closure

goal: workflow artifacts, commits созданы CX (3 atomic). Это финальный batch серии — inbox пуст, `.ready` удалён.
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-97.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-97.md` есть, inbox пуст (только Archive/);
- `.workflow/inbox/.ready` удалён окончательно;
- `.workflow/outbox/.done` JSON exit_code=0;
- **3 commits созданы CX через `git commit`**:
  - `batch-97: GATES-self-review — gate 2 + gate 3 self-review pass for single-user scope`;
  - `batch-97: DOCS-final-roadmap-closure — close cyrillic/AUTH3/API-hedge/gate items`;
  - `batch-97: closure — refresh .done`;
- `git status --short` возвращает только `?? CLAUDE.md`;
- `git add -f` для `.workflow/done/`, `.workflow/outbox/.done`, outbox reports.
visual_check: false
blocked_by: [DOCS-final-roadmap-closure]

---

## HARD RULES

- **ZERO production code changes**. Только `docs/` новые файлы и existing docs modifications. `src/`, tests, scripts, pyproject, config.py — не трогаем.
- **0 live Perplexity submits**.
- **Honest self-review**: если какой-то gate criterion не passes — это P0 finding для отдельного batch-98, gate status = "FAIL" или "PASS with conditions". Не замазывать реальные gap'ы.
- **Evidence required**: каждый PASS в gate checklist имеет file:line reference (не просто claim).
- **Commits обязательно создаёт CX** сам через `git commit` (lesson from batch-90/95/96 где CC пришлось retroactive landing'ить).
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: этот spec не будет переписан во время выполнения.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-97-{task-id}-report.md` + `.result.json` (Auto-Accept). GATES report фиксирует pass/fail per criterion + evidence; DOCS report — diff summary секций; closure report — SHA всех коммитов. GATES finding который FAIL — явно в report, не погребать.
