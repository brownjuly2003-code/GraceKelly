# Batch 98 — Gate 2 shallow-probe fix (lift PASS with conditions)
Date: 2026-04-23
Tier: XS (1 prod file + 1 test + 1 gate doc, 0 live quota)

Context (CX read-first):
- Plan ref: `docs/plans/2026-04-23-final-closure.md` (first batch of 98→99→100 chain).
- Gate 2 self-review (`docs/gates/2026-04-23-gate-2-operational-review.md:76-87`): criterion 2 marked `PASS with conditions` because `/healthz/ready` at `src/gracekelly/api/routes/health.py:379-384` только проверяет storage (`state.task_repository is None → 503`), полная readiness логика на `/api/v1/readiness` (`health.py:387+`).
- Current impl:
  ```python
  @router.get("/healthz/ready")
  async def readiness_probe(request: Request) -> dict[str, object]:
      state = get_app_state(request)
      if state.task_repository is None:
          raise HTTPException(status_code=503, detail="Storage unavailable")
      return {"status": "ok"}
  ```
- Kubernetes-style readiness probe should be **fast** (not run deep health checks), но **semantic** (return 503 если critical dependency не готов). Currently первое соблюдено (fast), второе — только частично (storage, not execution router).
- Fix design: добавить lightweight проверку что `app.state.execution_router` initialized AND (если browser enabled в settings) browser_adapter не None. **Не вызывать** `execution_router.healthcheck()` — это может ходить в `session_manager`/`automation` и замедлит probe. Просто attribute reads.
- Baseline: HEAD `3cdea66`, 2653 passed / 6 skipped / 97% coverage. ruff + mypy --strict clean. 0 live quota.

Goal: `/healthz/ready` gates на `task_repository AND execution_router AND (browser_enabled → browser_adapter populated)`; Gate 2 criterion 2 поднимается до unconditional PASS.

---

## Task: HEALTH-ready-probe-semantic

goal: `/healthz/ready` возвращает 503 если execution_router missing или browser_enabled=True но browser_adapter=None; latency probe не деградирует (no I/O beyond attribute read).
scope: `src/gracekelly/api/routes/health.py`, `tests/test_http_api.py` (или соответствующий тест-файл для `/healthz/ready` — CX делает Grep если имя отличается), `docs/gates/2026-04-23-gate-2-operational-review.md` — 3 файла.
done_when:
- **Health endpoint** (`src/gracekelly/api/routes/health.py:379-384`):
  - `readiness_probe` расширена: после `task_repository` check добавить:
    ```python
    if getattr(state, "execution_router", None) is None:
        raise HTTPException(status_code=503, detail="Execution router not initialized")
    if getattr(state.settings, "browser_enabled", False):
        if getattr(state, "browser_adapter", None) is None:
            raise HTTPException(status_code=503, detail="Browser adapter enabled but not initialized")
    ```
  - `getattr` с `None`/`False` defaults для безопасности (state может не иметь атрибута на edge-case initialization paths);
  - Если `state.settings.browser_enabled` attribute в config не `browser_enabled`, а какой-то `browser.enabled` или `GRACEKELLY_BROWSER_ENABLED`-derived field — CX делает Grep на config.py чтобы найти точное имя (likely `browser_enabled: bool` в Settings class), использовать его;
  - Не логировать на каждый probe hit (probe может fire многократно в секунду — логирование создаст шум);
  - Handler остаётся `async def`, не блокирующий (attribute reads — non-blocking).
- **Test** (`tests/test_http_api.py` или где уже есть `test_healthz_ready` / `test_readiness_probe` — Grep):
  - Новые тесты (3 штуки):
    1. `test_healthz_ready_503_when_execution_router_missing`: mock app.state с task_repository set, execution_router = None → status 503 detail contains "Execution router";
    2. `test_healthz_ready_503_when_browser_enabled_but_no_adapter`: state with task_repository + execution_router set, settings.browser_enabled=True, browser_adapter=None → 503 detail contains "Browser adapter";
    3. `test_healthz_ready_200_when_browser_disabled_and_no_adapter`: state with task_repository + execution_router set, settings.browser_enabled=False, browser_adapter=None → 200 OK (browser optional);
  - Existing `/healthz/ready` tests должны продолжать работать (если в них mock state имеет execution_router + соответствующие browser конфиги — ок; если нет — adapt minimally);
  - `R1 pass ≥ 2653 + 3 = 2656` full suite.
- **Gate 2 self-review doc update** (`docs/gates/2026-04-23-gate-2-operational-review.md`):
  - Criterion 2 section (`/healthz/ready gates on browser-adapter and storage readiness`): обновить "How verified" на новый reference line + status change от `PASS with conditions` to `PASS`;
  - `## Findings / deviations` section: deviation removed (или строка про shallow probe перемещена в "## Fixed follow-ups (2026-04-23 batch-98)" subsection с SHA ref);
  - `## Verdict` section: `PASS for single-user local deployment scope` (без "with conditions") после фикса;
  - Signature line обновить с "batch-98 updated" annotation.
- Gate verification: `ruff check src tests` clean; `.venv/Scripts/python.exe -m mypy src --strict` clean; scoped `pytest tests/test_http_api.py -q` passes.
- Zero изменений в: other routes, UI, scripts, other docs, config.py.
- **Commits обязательно создаёт CX** (lessons batch-90/95/96 — retroactive closure нежелателен).
visual_check: false
blocked_by: []

Notes:
- Если CX обнаружит что `Settings` имеет `browser.enabled` subfield а не flat `browser_enabled` — Grep `browser_enabled\|browser.enabled` в config.py покажет правильный accessor. Использовать тот же accessor что `main.py` при выборе инициализировать browser_adapter (consistency).
- `HTTPException` status_code=503 consistent с existing storage branch.
- CX сам решает test ordering within file (alphabetical / grouped / prepended).

---

## Task: closure-chain-next

goal: workflow closure, **self-chain `.ready`** т.к. batch-99 в inbox.
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-98.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-98.md` есть, inbox не содержит `batch-98.md`;
- commits созданы CX: `batch-98: HEALTH-ready-probe-semantic — lift gate 2 to unconditional PASS`, `batch-98: closure — refresh .done`;
- `.ready` **остаётся** в inbox (re-touch если был удалён) потому что batch-99 ещё pending — pipeline продолжается;
- `.workflow/outbox/.done` exit_code=0;
- `git status --short` только `?? CLAUDE.md`.
visual_check: false
blocked_by: [HEALTH-ready-probe-semantic]

---

## HARD RULES

- **Scope tight**: 3 файла. health.py + test_http_api.py + gate-2 doc.
- **Probe must stay fast**: no `healthcheck()` invocation, no DB query, no network call in `/healthz/ready` — only attribute reads.
- **0 live submits**.
- **Self-chain** `.ready` потому что batch-99 + batch-100 pending.
- **Commits обязательно CX** (2 atomic).
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: не будет переписан.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-98-*-report.md` + `.result.json`. HEALTH report: (a) exact state attribute names used, (b) test names, (c) before/after gate 2 criterion 2 status.
