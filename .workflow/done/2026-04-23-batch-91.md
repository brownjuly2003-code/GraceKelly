# Batch 91 — Live smoke harness expansion (Phase 2 closing)
Date: 2026-04-23
Tier: S-M (1 script + 1 docs + 1 new tests; 0 live quota в этом батче — harness сам manual-gated)

Context (CX read-first):
- Phase 2 (`docs/phased-roadmap.md:37-68`) status = `partial, first authenticated live-driver smoke proven`. Все исходные deliverables фактически закрыты кроме scalable live-smoke coverage: сейчас `scripts/live_smart_smoke.py` покрывает только 2 паттерна (`smart`, `debate`) и один EV-topic prompt. После batch-84/85/86/87/88/89/90 API-поверхность расширилась (`smart_v2`, `consensus`, `compare`, `upload`, fallback policy), но live harness не успевает — user проверяет эти endpoint'ы только через unit-мо́ки.
- Reference: текущий `scripts/live_smart_smoke.py` (226 lines): `PATTERN_MENU_LABEL`, `PATTERN_API_PATH`, `TOPIC_KEYWORDS`, `FORBIDDEN_MARKERS`, `DEFAULT_PROMPT`, `evaluate()`, `write_report()`. Manual-gated — запускается user'ом при необходимости (см. `inline SMOKE-live-smart/debate-acceptance` в Phase 17 Delivered).
- Decision: **расширить существующий harness** (не создавать новый). Единый entry-point, consistency reports, backward-compat с прошлыми `--pattern smart|debate` вызовами.
- Scope Phase 2 closing — **manual-gated harness, not scheduled/CI**. Single-user local deploy, user сам решает когда тратить квоту. Scheduled automation = out-of-scope (post-closing future batch).
- Baseline: HEAD `b683b23`, 2637 passed / 6 skipped / 0 failed / coverage 97%. ruff/mypy clean. 0 live Perplexity submits **в этом batch** (CX просто расширяет код + тесты evaluator'а — не гоняет живой smoke).
- Patterns to add:
  - **consensus** (`POST /api/v1/consensus`) — UI label в `Авто` группе `model-menu.js` нужно проверить (batch-82 перечисляет patterns). Если нет — paragraph в report "not surfaced in UI, skipping UI path" + direct POST через httpx как альтернативный smoke path.
  - **compare** (`POST /api/v1/compare`) — аналогично.
  - **upload** (`POST /api/v1/orchestrate/upload`) — file attachment flow. Требует `--attachment <path>` CLI flag + multipart body.
- Patterns to **skip**:
  - `fallback` path — требует env `GRACEKELLY_ENABLE_MODEL_FALLBACK=true` + искусственный failure primary browser модели. Ненатуральный live scenario. Покрыт unit-тестами batch-89. Документировать в runbook что fallback валидируется через unit-тесты, не live.
  - `smart/v2`, `batch`, `pipeline` — второстепенные, можно в future batch.

Goal батча: `scripts/live_smart_smoke.py` принимает `--pattern` ∈ {smart, debate, consensus, compare, upload}, pattern-specific evaluation (поля в response, topic keywords, min length), `--attachment` flag для upload; docs/operator-runbook.md имеет полную секцию "Live smoke harness" с usage examples per pattern и quota expectations; unit-тесты покрывают evaluator branching.

---

## Task: HARNESS-expand-patterns

goal: `scripts/live_smart_smoke.py` поддерживает 5 patterns (smart, debate, consensus, compare, upload) с pattern-specific evaluation; `--attachment` flag для upload; backward-compat с existing `smart`/`debate` вызовами; unit-тесты для evaluator.
scope: `scripts/live_smart_smoke.py`, `tests/test_live_smoke_harness.py` (NEW) — 2 файла.
done_when:
- **Parser extension** (`scripts/live_smart_smoke.py:62-70`):
  - `--pattern` choices расширены: `("smart", "debate", "consensus", "compare", "upload")`;
  - новый `--attachment` (optional, default None, type=str путь к файлу) — требуется только когда `--pattern=upload`. Если передан при другом pattern — WARN но не блокировать (ignored). Если `upload` без `--attachment` — argparse `error()` с понятным сообщением;
  - `--prompt` остаётся, default `DEFAULT_PROMPT` для smart/debate, для consensus/compare/upload default — pattern-specific (см. ниже);
- **Pattern maps** расширены (`scripts/live_smart_smoke.py:58-59`):
  - `PATTERN_MENU_LABEL` + новые ключи:
    - `"consensus": "Консенсус"` (если такой item есть в `static/js/model-menu.js`; если нет — `None` и harness выбирает "Умный выбор" как default pattern trigger или прямо POST через httpx);
    - `"compare": "Сравнение"` (аналогично);
    - `"upload": None` (upload не selectable через model-menu — прикрепление файла в composer);
  - `PATTERN_API_PATH` + новые:
    - `"consensus": "/api/v1/consensus"`;
    - `"compare": "/api/v1/compare"`;
    - `"upload": "/api/v1/orchestrate/upload"`;
- **Pattern-specific defaults** (новый dict `PATTERN_DEFAULT_PROMPT`):
  - smart, debate — текущий `DEFAULT_PROMPT` (EV markets);
  - consensus — короткий factual prompt, например `"Назови 3 крупнейших производителя электромобилей в Китае в 2024 году с краткими комментариями (1-2 предложения на каждого)"` (consensus работает лучше на factual, не opinionated);
  - compare — comparison prompt, например `"Сравни LLM Claude Sonnet 4.6 и GPT-5.4 по способности решать математические задачи с reasoning — дай структурированную оценку"`;
  - upload — prompt про содержимое файла, например `"Кратко опиши содержимое приложенного файла и выдели ключевые моменты"`.
- **Pattern-specific evaluation** (`evaluate()` расширен или factored в per-pattern helpers):
  - каждый pattern имеет свой dict в новой константе `PATTERN_EVALUATION`:
    ```python
    PATTERN_EVALUATION = {
        "smart":     {"min_length": 500, "topic_keywords": ("Europe", "China", "USA", "Европе", "Китае", "adoption", "subsidies"), "answer_fields": ("answer", "output_text")},
        "debate":    {"min_length": 500, "topic_keywords": ("Europe", "China", "USA", "Европе", "Китае", "adoption", "subsidies"), "answer_fields": ("improved_response", "answer")},
        "consensus": {"min_length": 300, "topic_keywords": ("электромобил", "производител", "Китае", "China"), "answer_fields": ("best_response", "consensus_text", "answer", "output_text")},
        "compare":   {"min_length": 400, "topic_keywords": ("Claude", "GPT", "reasoning"), "answer_fields": ("comparison", "analysis", "output_text")},
        "upload":    {"min_length": 150, "topic_keywords": (), "answer_fields": ("answer", "output_text")},  # пустой topic_keywords — content-specific, не проверяем
    }
    ```
  - `FORBIDDEN_MARKERS` применяется ко всем patterns (одинаковый);
  - `evaluate()` теперь берёт `PATTERN_EVALUATION[pattern]` и использует его поля; если `topic_keywords` пусто — проверка topic пропускается (только min_length + forbidden);
  - если answer не найден ни в одном из `answer_fields` — fail с reason `"answer field absent ({','.join(answer_fields)})"`.
- **Upload path** (новая функция `submit_upload(page, prompt, attachment_path)` или extension of `submit_prompt`):
  - для `--pattern=upload` harness не выбирает item из model-menu (upload не selectable через menu), а:
    - attach file через `#attachment-input` или аналогичный селектор (найти через `page.locator('input[type="file"]').first` если есть dedicated composer attachment path);
    - типит prompt;
    - submit;
  - Если UI path не находит attachment input — fallback на direct POST через httpx с `multipart/form-data` (prompt + file bytes). Harness сам решает, логирует выбор;
  - CX смотрит `docs/architecture.md` + `static/js/` для точных селекторов upload UI. Если неочевидно — использовать direct POST fallback и отметить в report как design choice.
- **Backward-compat**:
  - команда `python scripts/live_smart_smoke.py` (no flags, default pattern=smart) работает как до батча;
  - `--pattern smart|debate` + default prompt = тот же behavior;
  - existing PATTERN_API_PATH/LABEL/TOPIC_KEYWORDS/FORBIDDEN_MARKERS как raw constants могут быть сохранены или reshaped в новые dicts — важна совместимость с `evaluate()` contract (bool, reasons list).
- **Tests** (`tests/test_live_smoke_harness.py`):
  - (1) `test_evaluate_accepts_valid_smart_response`: response с `body_json["answer"]=<длинный EV-answer>` и status 200 → `(True, [])`;
  - (2) `test_evaluate_rejects_too_short_answer`: answer=100 chars → False + reason с `"answer too short"`;
  - (3) `test_evaluate_rejects_forbidden_marker`: answer содержит `"Ask a follow-up"` → False + reason с `"forbidden marker"`;
  - (4) `test_evaluate_rejects_missing_topic_keywords_for_smart`: smart answer без Europe/China/Европе — False + reason `"no topic keyword"`;
  - (5) `test_evaluate_accepts_consensus_with_new_keywords`: consensus answer с `"электромобил"` и длиной 350 — True;
  - (6) `test_evaluate_skips_topic_check_for_upload`: upload answer 200 chars без любых keywords — True (topic_keywords пусто);
  - (7) `test_evaluate_rejects_when_answer_field_absent`: body_json = `{"other_field": "..."}` для smart — False + reason `"answer field absent"`;
  - (8) `test_evaluate_rejects_non_200_status`: status_code=500, body valid — False + reason `"status_code=500"`;
  - (9) `test_pattern_defaults_match_matrix`: `PATTERN_DEFAULT_PROMPT` и `PATTERN_EVALUATION` содержат ключи для всех 5 patterns из `--pattern` choices — assertion на completeness;
  - (10) `test_argparse_upload_requires_attachment`: mock `parse_args(["--pattern", "upload"])` → SystemExit с non-zero exit (argparse.error triggered);
  - R1 pass ≥ 2637+10 = 2647; ruff clean; mypy clean через `.venv/Scripts/python.exe -m mypy src`;
  - Tests гонят **pure функции** (evaluate, parse_args mocks) — не запускают Playwright / httpx / uvicorn. Live harness сам — manual-gated.
- **Zero изменений** в `src/gracekelly/`, `static/`, `docs/architecture.md`, других scripts. Только `scripts/live_smart_smoke.py` + tests.
visual_check: false
blocked_by: []

Notes:
- `PATTERN_MENU_LABEL[pattern]` = None для upload → `select_pattern` должна рано return без клика (добавить guard в начале функции); pytest не должен падать на этой branch т.к. upload не через menu.
- Если CX не может найти menu-item для consensus/compare в UI — в `select_pattern` для этих patterns использовать тот же гвард (return None или raise `NotImplementedError` с clear message); report фиксирует это как "known UI gap — use --pattern smart as umbrella or direct POST".
- Direct-POST path для upload / когда UI не surface'ит pattern — keep simple httpx.post, не городить state machine. Harness принципиально прагматичен.
- Attachment для upload — CX сам подбирает тестовый small text файл (создать `.workflow/outbox/batch-91-test-attachment.txt` если не хочется тянуть существующий). Или принять путь от user через `--attachment`.

---

## Task: DOCS-operator-runbook-live-smoke

goal: `docs/operator-runbook.md` содержит section "## Live smoke harness" с preconditions, usage examples per pattern, quota expectations, report interpretation.
scope: `docs/operator-runbook.md`
done_when:
- Новая секция "## Live smoke harness" добавлена (точное место — решение CX, предпочтительно после existing automation-related секций, перед "## Troubleshooting" или эквивалент);
- Содержание:
  - **Preconditions**: (a) залогиненный Chrome profile в `D:/GraceKelly/chrome-profile/` или equivalent; (b) uvicorn запущен на `http://127.0.0.1:8011/` с `GRACEKELLY_BROWSER_ENABLED=true` и `GRACEKELLY_EXECUTION_PROFILE=hybrid`; (c) ни один chrome.exe процесс не использует профиль (PRE-CHECK команда PowerShell, как в batch-85/87 specs);
  - **Patterns supported** — таблица: pattern | API path | UI label | default prompt summary | expected quota | min answer length;
  - **Usage examples** — bash/PowerShell команды:
    - `python scripts/live_smart_smoke.py --pattern smart`
    - `python scripts/live_smart_smoke.py --pattern debate`
    - `python scripts/live_smart_smoke.py --pattern consensus`
    - `python scripts/live_smart_smoke.py --pattern compare`
    - `python scripts/live_smart_smoke.py --pattern upload --attachment <path>`
  - **Quota expectations**: smart — 1-3 submits (decomposition), debate — 3-5 submits (challenge/defense loop), consensus — 3-5 submits (multi-model voting), compare — 5 submits (FIVE_MODELS), upload — 1 submit;
  - **Report location**: `.workflow/outbox/<tag>-<PATTERN>-report.md` + `.workflow/outbox/<tag>-<PATTERN>-response.json`;
  - **Interpretation**: Status: success означает prompt→response end-to-end без AUTH_FAILED/shell-chrome; Status: failure имеет list of reasons; как читать response.json;
  - **Fallback coverage note**: "Fallback behaviour validated via unit-tests in `tests/test_router_fallback.py`, не через live harness — browser-adapter failure не воспроизводится искусственно";
  - **Not covered by this harness**: smart/v2, batch, pipeline — validated через unit-тесты и smoke routes `tests/test_routes_*`.
- Commit: `batch-91: DOCS-operator-runbook-live-smoke — add harness section for smart/debate/consensus/compare/upload`.
visual_check: false
blocked_by: [HARNESS-expand-patterns]

---

## Task: closure

goal: `.workflow/outbox/.done` exit_code=0, spec в `.workflow/done/`, `.ready` удалён, все commits созданы **сам CX** (в batch-90 CX забыл коммитить — не повторяем).
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-91.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-91.md` есть, inbox пуст;
- `.workflow/inbox/.ready` удалён;
- `.workflow/outbox/.done` JSON exit_code=0;
- **commits созданы через `git commit`** (не только описаны в reports): `batch-91: HARNESS-expand-patterns — <phrase>`, `batch-91: DOCS-operator-runbook-live-smoke — <phrase>`, `batch-91: closure — refresh .done` (+ возможно `— remove .ready`);
- `git status --short` возвращает только `?? CLAUDE.md`;
- `git add -f` для `.workflow/done/`, `.workflow/outbox/.done`, `.workflow/outbox/2026-04-23-batch-91-*.{md,result.json,log}`.
visual_check: false
blocked_by: [DOCS-operator-runbook-live-smoke]

---

## HARD RULES

- **Scope tight**: 3 файла. `scripts/live_smart_smoke.py`, `tests/test_live_smoke_harness.py`, `docs/operator-runbook.md`. Zero в `src/gracekelly/`, `static/`, others.
- **0 live Perplexity submits в этом batch** — CX расширяет код, тестирует evaluator, пишет docs. Реальный live run — отдельный manual run user'ом после landing.
- **Backward-compat**: `python scripts/live_smart_smoke.py` без флагов работает как раньше (pattern=smart, EV prompt).
- **Commits обязательно созданы CX**: последний batch-90 закончил с `коммиты не создавал` — CC пришлось коммитить вручную. В этом batch'е CX сам делает 3-4 atomic commits по спеке.
- **Fallback pattern НЕ добавляется в harness** — обоснование в docs.
- mypy через `.venv/Scripts/python.exe -m mypy src`. ruff `ruff check src tests` (+scripts если они в ruff scope, проверь config). Если scripts/ вне ruff scope — не расширяй.
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: этот spec не будет переписан во время выполнения.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-91-{task-id}-report.md` + `.result.json` (Auto-Accept). HARNESS report фиксирует: (a) какие patterns added, (b) какие UI-labels обнаружены vs. fallback на direct POST, (c) attachment strategy (dedicated test file vs. user-provided), (d) test id'ы. DOCS report — diff summary секции. Closure report — SHA всех коммитов батча.
