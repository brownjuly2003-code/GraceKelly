# Batch 84 — Unify browser-adapter fallback across smart_v2 / consensus / compare
Date: 2026-04-22
Tier: M (1 backend task covering 3 routes + shared helper, no live quota required)

Context (CX read-first):
- Batch 80 (`dd632c7 batch-80: BACKEND-smart-debate-browser-support`) починил `src/gracekelly/api/routes/smart.py` и `src/gracekelly/api/routes/debate.py` — browser-backed модель (`model_spec.adapter_kind == "browser"`, provider `perplexity`) теперь идёт через `state.browser_adapter`, step backend = `ExecutionBackend.BROWSER`.
- Остались 3 route'а с тем же pre-batch-80 багом — `state.api_adapters.get(model_spec.provider)` без fallback на browser:
  - `src/gracekelly/api/routes/smart_v2.py:91`
  - `src/gracekelly/api/routes/consensus.py:121`
  - `src/gracekelly/api/routes/compare.py` (compare берёт `models: list[str]`; логика ищет адаптер для каждого)
- Ни один из них сейчас не достижим из UI model-menu (smart_v2/consensus/compare нет как items), но `/api/v1/smart/v2`, `/api/v1/consensus`, `/api/v1/compare` остаются в API table (README) — внешний caller с `model="best"` получит `400 No API adapter for provider 'perplexity'.`, идентичное pre-batch-80 поведение.
- В batch-80 fix логика ветвления по `adapter_kind` была inline в каждом route. Для 3 routes это дублирование — допустим shared helper в `src/gracekelly/api/routes/_helpers.py` (создать если нет) или `src/gracekelly/api/adapter_lookup.py`.
- Никаких UI / live Perplexity изменений в этом батче — только backend + unit tests.
- mypy src локально `Success: no issues found in 104 source files`. Не трогать `middleware.py:120` (он не существует).
- Baseline: HEAD `bda2d2e`, full suite 2587 passed / 6 skipped.

Goal батча: все 4 route'а (smart, debate, smart_v2, consensus, compare) используют единый lookup, закрывая browser-adapter gap для smart_v2/consensus/compare ровно как batch-80 закрыл его для smart/debate.

---

## Task: BACKEND-unify-browser-adapter

goal: `/api/v1/smart_v2`, `/api/v1/consensus`, `/api/v1/compare` принимают browser-backed модель (`model="best"` или другой с `adapter_kind="browser"`) через `state.browser_adapter`; smart.py и debate.py переведены на тот же shared helper; API-path и degraded-provider ошибки не регрессируют.
scope: src/gracekelly/api/routes/smart.py, src/gracekelly/api/routes/debate.py, src/gracekelly/api/routes/smart_v2.py, src/gracekelly/api/routes/consensus.py, src/gracekelly/api/routes/compare.py, src/gracekelly/api/routes/_helpers.py (или src/gracekelly/api/adapter_lookup.py — CX решает), tests/test_smart_route.py, tests/test_debate_route.py, tests/test_smart_v2_route.py (если файл есть — иначе создать), tests/test_consensus_route.py (аналогично), tests/test_compare_route.py (аналогично)
done_when: [shared helper экспортирует функцию типа `resolve_execution_adapter(state, model_spec, dry_run) -> tuple[ExecutionAdapter, ExecutionBackend]` либо аналогичный контракт — CX решает сигнатуру по smart.py текущему коду; smart.py + debate.py переведены на helper (patch должен оставить их тесты зелёными без правки); smart_v2.py и consensus.py переведены на helper (один `model`); compare.py переведён на helper с loop по `models: list[str]` (каждая модель независимо резолвится через helper, ошибка сообщает индекс+id); новые/расширенные unit-тесты для каждого route: (a) browser-backed `model="best"` с подстановкой `state.browser_adapter` возвращает 200, (b) API-model без сконфигурированного adapter'а → 400 с осмысленным detail, (c) browser-backed без `state.browser_adapter` → 400; compare также covers mixed list (API + browser); R5 kill для каждой задачи (временный возврат к `api_adapters.get(...)` в одном route ломает свой тест); `R1 pass ≥ 2590` (+3 минимум — по одному новому browser-test на route), `ruff`+`mypy src` clean; mypy не придумывать; docs/phased-roadmap.md не трогать в этом таске (отдельный DOCS-task в конце)]
visual_check: false
blocked_by: []

Notes:
- Compare-loop может выкидывать 400 на первой непригодной модели или собирать все проблемы — смотреть существующее поведение, НЕ менять error shape.
- Если shared helper удобно положить в `src/gracekelly/core/adapter_selection.py` — тоже OK, важнее не затащить circular imports.
- Смотреть `src/gracekelly/app_state.py` для типа `browser_adapter` — он `Any`, не протокол; осторожно с `None`-guard'ом.

---

## Task: DOCS-phase-17-remaining-update

goal: В `docs/phased-roadmap.md` Phase 17 Remaining — пункт про smart_v2/consensus/compare browser gap закрыт этим батчем.
scope: docs/phased-roadmap.md
done_when: [строка `/api/v1/smart_v2, /api/v1/consensus, /api/v1/compare have the same browser-adapter gap` удалена из Phase 17 Remaining; добавлена строка в Phase 17 Delivered: `batch-84 BACKEND-unify-browser-adapter: shared adapter-lookup helper across smart/debate/smart_v2/consensus/compare; three routes now accept browser-backed models` с SHA из `git log`; `Last updated` остаётся `2026-04-22` если дата не менялась; README не трогать; коммит `batch-84: DOCS-phase-17-remaining-update — unify helper now closes smart_v2/consensus/compare gap`]
visual_check: false
blocked_by: [BACKEND-unify-browser-adapter]

---

## HARD RULES

- Scope строго: 5 route файлов + shared helper + 5 test files + roadmap. Не трогать static/, chat.js, static/js/model-menu.js.
- Live Perplexity НЕ используется в этом батче.
- CLAUDE.md не трогать.
- mypy src локально clean — не изобретать `middleware.py:120`.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Коммиты atomic: `batch-84: BACKEND-unify-browser-adapter — <phrase>`, `batch-84: DOCS-phase-17-remaining-update — <phrase>`, `batch-84: closure — refresh .done`.

---

## Отчёты
`.workflow/outbox/2026-04-22-batch-84-{task-id}-report.md` + `.result.json` (Auto-Accept). После всех задач — `.workflow/outbox/.done` обновить, batch-84.md → `.workflow/done/`, `.ready` очистить.
