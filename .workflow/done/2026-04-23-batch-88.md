# Batch 88 — Scoped-menu-search verification vs. Orchestrator2 + close Best-alias (or keep)
Date: 2026-04-23
Tier: S (CX выступает verifier + decider; 1-2 regression tests + docs; 0 live quota)

Context (CX read-first):
- Batch-87 landed scoped-menu-search в `PlaywrightBrowserAutomation.select_model` (commit `f430d39`): ищет option сначала внутри `model_menu_candidates` через `locator(selector).locator(f"text={requested}").first`, потом role-filtered (`[role="menuitemradio"]:has-text(..)` и др.), потом legacy global fallback. Unit-тесты проверяют что каждый из трёх путей срабатывает, но **не проверяют** семантику multi-match scenario внутри scope (несколько visible matches → `.first` берёт верхний по DOM order).
- Reference implementation: `D:/Perplexity_Orchestrator2/browser/model_selection.py:182-240`. Используют `menu_scope = page.locator('[role="menu"], [role="listbox"], [data-radix-popper-content-wrapper]')` → `menu_scope.locator(f'text={model_name}').first` → `click(timeout=3000)`. Этот pattern в проде на ~30 Perplexity Pro аккаунтов (см. `D:/Perplexity_Orchestrator2/CLAUDE.md`). Работает стабильно включая выбор "Best" (config/models.py:19 `BEST = "Best"`).
- Recon bundle `D:/GraceKelly/tmp/browser-recon/2026-04-23/recon-03-model-menu-attrs.json` фиксирует реальный DOM "Best" item (commit `9203f50`):
  ```
  <div role="menuitemradio" data-state="unchecked">
    <div class="flex-1 grid ...">
      ...
      <div class="col-start-2 min-w-0"><div class="flex..."><div class="min-w-0">
        <span translate="no">Best</span>                                  <!-- match #1 -->
      </div></div></div>
      ...
      <span class="text-quiet ...">Selects the best available model</span><!-- match #2: contains "best" -->
    </div>
  </div>
  ```
  Playwright `text=Best` (case-insensitive substring, whitespace-normalized) матчит оба: `<span translate="no">Best</span>` и `<span class="text-quiet">Selects the best available model</span>`. `.first` по DOM order → первый span ("Best" exact). Bubbling клика поднимается к `menuitemradio` parent.
- Recon decision: `SELECTOR_DISAMBIGUATE` — якорить к `[role="menuitemradio"]` с exact child text. Текущий fix **не якорит** явно, полагается на scope + DOM order + `.first`. Open question: достаточно ли этого, или нужен explicit anchor к `menuitemradio` с `text_exact`.
- **Orchestrator2 работающий ответ на этот же вопрос**: они тоже не якорят к `menuitemradio` явно. Используют `menu_scope.locator(f'text={name}').first.click()`. Значит pattern consistent с нашим — если работает у них, должен работать у нас.
- Baseline: HEAD `6f04a03`, 2611 passed / 6 skipped / 0 failed. `mypy src` clean через `.venv/Scripts/python.exe -m mypy src` (не глобальный `python -m mypy` — тот цепляет unrelated `middleware.py:120` на Python 3.13, см. batch-87 CX note). `ruff check src tests` clean.
- 0 live Perplexity submits. Чистая семантическая верификация + unit coverage.

Goal батча: **ответить на вопрос** "нужен ли explicit anchor к `[role="menuitemradio"]` + exact text, или scoped-menu-search + `.first` достаточно". CX принимает решение сам на основе сравнения с Orchestrator2 и regression coverage. Итог: либо Best-alias Remaining пункт закрыт (move to Delivered), либо остаётся с обновлённой аннотацией "validation requires live smoke + reason".

---

## Task: VERIFY-scoped-menu-vs-orchestrator2

goal: CX сравнивает scoped-menu-search (f430d39) с reference pattern в Orchestrator2, выдаёт вердикт `PATTERN_EQUIVALENT` или `DIVERGES` с обоснованием, и добавляет regression-тесты симулирующие multi-match scenario внутри menu scope.
scope: `tests/test_playwright_driver.py` (добавить regression tests, не править существующие), `.workflow/outbox/2026-04-23-batch-88-VERIFY-scoped-menu-vs-orchestrator2-report.md`
done_when:
- CX прочитал `D:/Perplexity_Orchestrator2/browser/model_selection.py` строки 175-245 (scope + click + fallbacks) и `D:/Perplexity_Orchestrator2/browser/selectors.py` (что у них в model_menu_candidates / model_menu_item_selector аналогах);
- CX прочитал `D:/GraceKelly/src/gracekelly/adapters/browser/playwright_driver.py:_find_option_in_menu_scope` и `_find_option_via_role_filter` (commit `f430d39`);
- В report отражено по 3 пунктам:
  - (a) **Menu scope selector**: Orchestrator2 vs GraceKelly — одинаковые? (Orchestrator2: `'[role="menu"], [role="listbox"], [data-radix-popper-content-wrapper]'` одним массивом; GraceKelly: `PerplexitySelectors.model_menu_candidates` кортеж `('[data-radix-popper-content-wrapper]', '[role="dialog"]', '[role="listbox"]', '[role="menu"]')`). Отметить что у нас есть `[role="dialog"]` и нет у них, у них нет `[role="menu"]` в том же массиве — но batch-87 добавил. Критично ли это расхождение?
  - (b) **Primary option locator**: Orchestrator2 `menu_scope.locator(f'text={name}').first` vs GraceKelly `page.locator(selector).locator(f'text={requested}')` (GraceKelly перебирает scope по одному, берёт first-visible из первого scope где нашёлся; Orchestrator2 объединяет scopes в один `.locator(', ')`). Семантически эквивалентны при order preservation? Есть разница в случае когда `[data-radix-popper-content-wrapper]` содержит два menu (маловероятно)?
  - (c) **Fallback**: Orchestrator2 — role-filter + JS `.evaluate('(el) => el.click()', target_item)`; GraceKelly — role-filter + global page.get_by_role/text. Orchestrator2 более агрессивен в fallback, GraceKelly — проще. Риски?
- CX выдаёт вердикт одного из двух (в одно предложение + 1-2 обоснования):
  - **`PATTERN_EQUIVALENT`** — scoped-menu-search семантически эквивалентен Orchestrator2, nested-text-node risk закрыт через scope + DOM order + `.first`. Best-alias можно считать закрытым на уровне unit coverage, live smoke — best-effort validation, не blocker.
  - **`DIVERGES`** — есть material difference (перечислить), который означает что GraceKelly fix не эквивалентен. Нужен follow-up patch (описать какой).
- Regression tests добавлены в `tests/test_playwright_driver.py` (class `TestFindOptionInMenuScope` или аналог):
  - (1) `test_find_option_in_menu_scope_returns_first_when_multiple_text_matches`: mock `page.locator("[data-radix-popper-content-wrapper]")` возвращает scope, `scope.locator("text=Best")` возвращает locator у которого `.first` — visible элемент с `inner_text()=="Best"` (exact span), `.is_visible()==True`; `.all()` или эквивалент возвращает 2 candidates (второй — "Selects the best available model"). Проверить что helper возвращает именно `.first`, не итерирует дальше.
  - (2) `test_find_option_in_menu_scope_returns_none_when_all_scopes_empty`: mock page.locator для всех `model_menu_candidates` возвращает scope чей inner `.locator("text=<name>")` не visible → helper возвращает None;
  - (3) `test_find_option_in_menu_scope_prefers_earlier_scope_selector`: `[data-radix-popper-content-wrapper]` scope имеет visible match, `[role="menu"]` scope тоже имеет match (с другим элементом) — helper возвращает из первого scope (model_menu_candidates порядок соблюдён);
  - Tests используют тот же mock-стиль, что существующие тесты select_model в `tests/test_playwright_driver.py` (классы уже есть — не дублировать helpers);
- `R1 pass ≥ 2614` (+3 regression tests); `R3` на `tests/test_playwright_driver.py` ≥ 60+3 passed; `ruff check src tests` clean; `mypy src` через `.venv/Scripts/python.exe -m mypy src` → `Success`;
- Zero изменений в `src/` (только tests + report). Если CX решит что `DIVERGES` — описать fix в report, сам fix — не в этом батче.
visual_check: false
blocked_by: []

Notes:
- CX волен добавить 4-ый тест если считает что есть ещё важный edge case (например, `role_filter` helper также заслуживает multi-match regression). Scope стабилен: только `tests/test_playwright_driver.py`.
- Если CX обнаруживает в Orchestrator2 существенный pattern, который мы упустили (например, они после клика ждут `await asyncio.sleep(0.5)` и re-check menu visibility — строки 208-228) — отметить в report как `FUTURE_HARDENING`, не делать в этом батче.
- Vendor-docs Playwright text engine: `text=foo` — case-insensitive substring match over normalized whitespace; `text="foo"` (с кавычками) — exact-match. `.first` — first by DOM order. `:has-text(..)` — substring match on any descendant text. Использовать в обосновании.

---

## Task: DOCS-close-or-keep

goal: `docs/phased-roadmap.md` обновлён согласно вердикту из VERIFY-таска. Либо Best-alias Remaining → Delivered с аннотацией "validated via reference pattern equivalence + unit coverage", либо остаётся в Remaining с обновлённой причиной.
scope: `docs/phased-roadmap.md`
done_when:
- Если вердикт `PATTERN_EQUIVALENT`:
  - Существующая строка `**Browser adapter non-deterministic selection for the `"Best"` alias** — ...` **удалена** из Phase 17 Remaining (полностью, включая под-текст про validation pending);
  - В Phase 17 Delivered добавлена одна строка: `**batch-88 VERIFY-scoped-menu-vs-orchestrator2**: scoped-menu-search pattern verified equivalent to `D:/Perplexity_Orchestrator2/browser/model_selection.py:182-240` (production on ~30 Perplexity Pro accounts); multi-match nested-text-node behavior covered by regression tests — Best-alias Remaining closed (`<SHA>`)`;
- Если вердикт `DIVERGES`:
  - Строка в Remaining **остаётся**, но под-текст заменяется на: `— CX verification 2026-04-23 (batch-88 `<SHA>`) found divergence from Orchestrator2 reference: <one-line summary>. Follow-up patch scheduled as separate batch. Recon findings + decision: docs/perplexity-dom-recon.md#best-alias-recon-2026-04-23.`
- Остальные два Remaining пункта (cyrillic, AUTH3) нетронуты;
- README, operator-runbook, perplexity-dom-recon.md не трогать;
- Commit: `batch-88: DOCS-close-or-keep — <verdict>`.
visual_check: false
blocked_by: [VERIFY-scoped-menu-vs-orchestrator2]

---

## Task: closure

goal: `.workflow/outbox/.done` exit_code=0, spec в `.workflow/done/`, `.ready` удалён.
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-88.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-88.md` существует, `.workflow/inbox/2026-04-23-batch-88.md` нет;
- `.workflow/inbox/.ready` удалён;
- `.workflow/outbox/.done` JSON `{"completed_at": "<ISO-8601>", "exit_code": 0}`;
- commits atomic: `batch-88: VERIFY-scoped-menu-vs-orchestrator2 — <verdict>`, `batch-88: DOCS-close-or-keep — <verdict>`, `batch-88: closure — refresh .done` (+ возможен `— remove .ready` как в batch-86/87 если tracked deletion всплыл);
- `git add -f` для tracked файлов в `.workflow/done/`, `.workflow/outbox/.done`, `.workflow/outbox/2026-04-23-batch-88-*.{md,result.json}`.
visual_check: false
blocked_by: [DOCS-close-or-keep]

---

## HARD RULES

- **0 изменений в `src/`**. Только `tests/test_playwright_driver.py` (new regression tests) и `docs/phased-roadmap.md`. Никакого production кода.
- **0 live Perplexity submits**. Даже если CX сомневается — sit with it, использовать reference equivalence + unit coverage.
- **CX автономно принимает вердикт.** Не запрашивать подтверждения у CC — решение CX документирует в report.
- Если CX видит, что для принятия решения нужны данные из live smoke (не закрывается рассуждением) — вердикт = `DIVERGES` с reason `LIVE_SMOKE_NEEDED`, Remaining остаётся, follow-up batch для live smoke.
- CLAUDE.md не трогать. mypy/ruff gates обязательны (mypy через `.venv/Scripts/python.exe -m mypy src`, не глобальный).
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- **Spec stability**: этот спек не будет переписан в процессе. CX может подхватывать в любой момент и выполнять полностью (lesson from batch-87 race).

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-88-{task-id}-report.md` + `.result.json` (Auto-Accept).
