# Batch 89 — Model fallback policy (Phase 4 deliverable)
Date: 2026-04-23
Tier: S-M (config + core contract + router + tests; 0 live quota)

Context (CX read-first):
- Phase 4 Deliverables (`docs/phased-roadmap.md:95-100`) из 5 исходных пунктов остались два открытых: `model fallback policy` и `request budget`. Этот батч закрывает `model fallback policy`. Бюджеты — отдельный батч.
- Текущее поведение в `src/gracekelly/core/router.py:154-172` (`ExecutionRouter._dispatch_step`): если `step.backend == ExecutionBackend.API` но адаптера для провайдера нет → возвращает `FailureCode.PROVIDER_UNAVAILABLE`; если backend=BROWSER и `self._browser_adapter is None` → аналогично. В остальных случаях дёргает single adapter.execute и возвращает его результат. **Нет retry на альтернативную модель / адаптер при browser-side failures** (AUTH_FAILED, PROVIDER_UNAVAILABLE, TIMEOUT).
- Симптом: когда Perplexity UI падает посреди smart-decomposition (см. Phase 17 incidents — batch-81 "best" alias, batch-83 UTF-8 harness timeout), весь step помечается FAILED. При включённом fallback семантически эквивалентная API модель (`claude-sonnet-4-6-api` для `claude-sonnet-4-6` browser) могла бы спасти выполнение.
- Reference patterns:
  - `D:/Perplexity_Orchestrator2` использует browser+API в параллель (juhub делает 4 Perplexity + 4 API simultaneously, см. `D:/Perplexity_Orchestrator2/CLAUDE.md`). Это **не fallback chain**, а параллельная redundancy. Для GraceKelly per-step fallback экономнее (секвенциальный retry без удвоения квоты).
  - Phase 4 доставленное: AccountPool + `retry_of_task_id` (task-level retry). Model fallback — **step-level**, ортогонален task-level retry.
- Каталог моделей (`src/gracekelly/core/models.py:8-84`):
  - `ModelSpec` fields: id, display_name, aliases, adapter_kind, provider, provider_model_id, timeout_seconds, expected_latency_class, concurrency_limit, reasoning_capable. **Нет fallback_model_id поля — его нужно добавить.**
  - API-модели уже в каталоге: `mistral-small` (Mistral), `gpt-5-4-api` (OpenAI), `claude-sonnet-4-6-api` (Anthropic). Browser-модели: best, sonar, claude-sonnet-4-6, gpt-5-4, gemini-3-1-pro, kimi-k2-5, claude-opus-4-6, max, nemotron-3-super.
  - Очевидные fallback-пары: `claude-sonnet-4-6` (browser) → `claude-sonnet-4-6-api` (anthropic), `gpt-5-4` (browser) → `gpt-5-4-api` (openai). Sonar/Best/Gemini/Kimi/Opus/Max/Nemotron — без API-эквивалента в текущем каталоге, остаются без fallback (`fallback_model_id=None`).
- `FailureCode` (`src/gracekelly/core/contracts.py:13-20`): AUTH_FAILED, MODEL_MISMATCH, PROVIDER_UNAVAILABLE, TIMEOUT, RATE_LIMITED, STORAGE_FAILED, UNKNOWN_ERROR. **Fallback должен триггериться только на подмножество**: AUTH_FAILED, PROVIDER_UNAVAILABLE, TIMEOUT (UNKNOWN_ERROR — опционально, под флагом). **НЕ триггерить** на RATE_LIMITED (concurrency gate, fallback не поможет), MODEL_MISMATCH (семантическая ошибка), STORAGE_FAILED (инфраструктурная).
- Baseline: HEAD `1af8304`, 2614 passed / 6 skipped / 0 failed / coverage 97%. mypy clean через `.venv/Scripts/python.exe -m mypy src`. ruff clean.
- 0 live Perplexity submits. Fix полностью unit-testable mock'ами adapter'ов.

Goal батча: `ExecutionRouter._dispatch_step` при получении от primary adapter result'а с failure_code ∈ fallback-triggering codes → находит `step.model.fallback_model_id` в каталоге, строит новый ExecutionStep с fallback моделью и её backend'ом, дёргает соответствующий adapter, возвращает result с пометкой `details["fallback_used"]=True, "fallback_from_model"=<orig>, "fallback_reason"=<code>, "primary_failure_message"=<msg>`. Feature за env-flag `GRACEKELLY_ENABLE_MODEL_FALLBACK` (default false — backward-compat).

---

## Task: CORE-model-fallback-policy

goal: `ModelSpec.fallback_model_id: str | None` поле добавлено; каталог моделей прописывает fallback для browser-моделей с API-эквивалентом; `ExecutionRouter._dispatch_step` при failure_code ∈ {AUTH_FAILED, PROVIDER_UNAVAILABLE, TIMEOUT} и включённом env-флаге делает ровно одну попытку fallback на указанную модель, возвращает enriched result; behavioral unchanged при default env-flag=false.
scope: `src/gracekelly/core/models.py`, `src/gracekelly/core/router.py`, `src/gracekelly/config.py`, `tests/test_router_fallback.py` (новый файл) — 4 файла.
done_when:
- **ModelSpec extension** (`src/gracekelly/core/models.py`):
  - `fallback_model_id: str | None = None` добавлено после `reasoning_capable`;
  - В catalog builder (функция/константа где список browser-моделей строится — найти через Grep `claude-sonnet-4-6"` в models.py) у `claude-sonnet-4-6` browser prop задан `fallback_model_id="claude-sonnet-4-6-api"`, у `gpt-5-4` browser — `fallback_model_id="gpt-5-4-api"`. Остальные browser-модели — без fallback (None по умолчанию).
  - API-модели (`mistral-small`, `gpt-5-4-api`, `claude-sonnet-4-6-api`) — `fallback_model_id=None` (API-to-browser fallback не делаем — браузер менее надёжен).
  - Если `ModelCatalogSnapshot` serialize/deserialize где-то читает поля (Grep `payload["concurrency_limit"]` в models.py → line ~219 — там есть десериализация): добавить optional десериализацию `fallback_model_id` (default None для старых snapshots), BC безопасно.
- **Config** (`src/gracekelly/config.py`): добавить `enable_model_fallback: bool = False` + env parse `GRACEKELLY_ENABLE_MODEL_FALLBACK` через существующий `_env_bool` или equivalent (Grep `_env_bool\|_env_flag` в config.py). Документировать в README/env.example НЕ нужно — это отдельный DOCS батч позже.
- **Router change** (`src/gracekelly/core/router.py`):
  - Новый private helper `_try_fallback(step, primary_result, request) -> ExecutionResult | None`:
    - если `settings.enable_model_fallback is False` → return None;
    - если `primary_result.failure_code not in {FailureCode.AUTH_FAILED, FailureCode.PROVIDER_UNAVAILABLE, FailureCode.TIMEOUT}` → return None;
    - если `step.model.fallback_model_id is None` → return None;
    - lookup `fallback_model = next((m for m in list_models() if m.id == step.model.fallback_model_id), None)`; если None → return None (безопасно, no-op);
    - построить `fallback_step = ExecutionStep(model=fallback_model, backend=ExecutionBackend(fallback_model.adapter_kind), provider=fallback_model.provider, provider_model_id=fallback_model.provider_model_id, step_index=step.step_index)`;
    - построить `fallback_request = replace(request, step=fallback_step)`;
    - вызвать `self._dispatch_step(fallback_step, fallback_request)` — но чтобы избежать рекурсивного fallback (fallback модели сами `fallback_model_id=None`, так что рекурсия естественно останавливается); **НЕ делать второй уровень fallback** (single-shot policy).
    - обогатить result: `replace(result, details={**result.details, "fallback_used": True, "fallback_from_model": step.model.id, "fallback_reason": primary_result.failure_code.value, "primary_failure_message": primary_result.failure_message or ""})`.
    - вернуть enriched result.
  - В `_dispatch_step` перед `return` добавить:
    ```python
    if result.status == StepStatus.FAILED:
        fallback_result = self._try_fallback(step, result, request)
        if fallback_result is not None:
            return fallback_result
    return result
    ```
  - Точка вызова — после `_execute_with_concurrency_limit` и для API-path, и для BROWSER-path. **НЕ** делать fallback для `_missing_adapter_result` early-return на line 158-163 — там это не intended adapter failure, а конфигурационный gap; если CX считает иначе — обосновать в report. Default: fallback только post-execute.
- **Tests** (`tests/test_router_fallback.py`):
  - Используй fake `ExecutionAdapter` классы (см. паттерн в существующих router тестах — Grep `_FakeAdapter\|FakeAdapter\|test_router` в tests/);
  - (1) `test_fallback_disabled_by_default_even_with_fallback_id`: primary adapter возвращает PROVIDER_UNAVAILABLE, `settings.enable_model_fallback=False` → fallback НЕ срабатывает, результат = primary (no `fallback_used` в details);
  - (2) `test_fallback_triggers_on_auth_failed_when_enabled`: primary browser adapter → AUTH_FAILED, API fallback adapter → COMPLETED, env on → результат = fallback result с `details["fallback_used"]=True`, `fallback_from_model="claude-sonnet-4-6"`, `fallback_reason="auth_failed"`, `primary_failure_message=<non-empty>`;
  - (3) `test_fallback_triggers_on_provider_unavailable_when_enabled`: аналог (2) для PROVIDER_UNAVAILABLE;
  - (4) `test_fallback_triggers_on_timeout_when_enabled`: аналог (2) для TIMEOUT;
  - (5) `test_fallback_does_not_trigger_on_rate_limited`: primary → RATE_LIMITED, env on → no fallback, результат = primary;
  - (6) `test_fallback_does_not_trigger_on_model_mismatch`: primary → MODEL_MISMATCH, env on → no fallback, результат = primary;
  - (7) `test_fallback_does_not_trigger_without_fallback_model_id`: model (`sonar` или аналог) без fallback_model_id, primary → AUTH_FAILED, env on → no fallback, результат = primary;
  - (8) `test_fallback_no_second_level_on_fallback_failure`: primary → AUTH_FAILED, fallback adapter тоже fails (say PROVIDER_UNAVAILABLE), env on → результат = fallback failure (не primary; details показывают что fallback attempted и failed), НО без попыток третьего уровня (recursion prevented via `fallback_model.fallback_model_id=None`);
  - (9) `test_model_spec_fallback_field_serializes`: `asdict(ModelSpec(..., fallback_model_id="x"))` содержит `"fallback_model_id": "x"`; десериализация payload со старым форматом (без поля) даёт `fallback_model_id=None`;
  - `R1 pass ≥ 2614 + 9 = 2623` на полном pytest; `R3` scoped (`tests/test_router_fallback.py tests/test_router.py` если существует) clean;
  - `ruff check src tests` clean; `.venv/Scripts/python.exe -m mypy src` → `Success`.
- **Zero changes** в: routes (`src/gracekelly/api/routes/`), UI (`static/`), docs, scripts, `.workflow/`, adapters themselves (browser/api adapter implementations).
visual_check: false
blocked_by: []

Notes:
- Если CX видит что `list_models()` не имеет эффективного lookup-by-id (линейный scan acceptable на 10-20 моделей), не оптимизировать — преждевременно. Helper `_find_model_by_id` в том же файле, приватный.
- Если `replace(request, step=fallback_step)` не работает из-за frozen dataclass constraints на поле `step` — проверить: `ExecutionRequest` frozen (contracts.py:145), `replace` из dataclasses работает для frozen. Если всплывает type issue — использовать `dataclasses.replace` explicitly.
- `ExecutionBackend(fallback_model.adapter_kind)` работает если adapter_kind ∈ {"api", "browser"} (строковые значения enum). Mistral/OpenAI/Anthropic модели имеют `adapter_kind="api"` — see `_API_MODEL_SPECS` в models.py:47-84. Safe.
- Concurrency gate (`_execute_with_concurrency_limit`) для fallback модели — lookup по `fallback_model.id`, concurrency_limit берётся из её ModelSpec. Естественно, если идёт через `_dispatch_step`. Проверь что рекурсивный вызов `_dispatch_step` реально попадает в `_execute_with_concurrency_limit` — через API-path или BROWSER-path в зависимости от `fallback_step.backend`.

---

## Task: closure

goal: `.workflow/outbox/.done` exit_code=0, spec в `.workflow/done/`, `.ready` удалён.
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-89.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-89.md` есть, `.workflow/inbox/2026-04-23-batch-89.md` нет;
- `.workflow/inbox/.ready` удалён;
- `.workflow/outbox/.done` JSON с `exit_code=0`;
- commits atomic: `batch-89: CORE-model-fallback-policy — <phrase>`, `batch-89: closure — refresh .done` (+ возможен `— remove .ready` если tracked deletion);
- `git add -f` для tracked файлов в `.workflow/done/`, `.workflow/outbox/.done`, `.workflow/outbox/2026-04-23-batch-89-*.{md,result.json}`.
visual_check: false
blocked_by: [CORE-model-fallback-policy]

---

## HARD RULES

- **Scope tight**: 4 файла и только. `core/models.py`, `core/router.py`, `config.py`, `tests/test_router_fallback.py`. Если CX видит что нужно трогать что-то ещё (например, `core/contracts.py`) — STOP + diagnostic в report.
- **0 live Perplexity submits.** Все тесты с mock adapter'ами.
- **Backward-compat critical**: `GRACEKELLY_ENABLE_MODEL_FALLBACK` default false. Существующие router-тесты (без env-override) должны пройти unchanged. Если какой-то тест падает из-за dataclass signature change (add fallback_model_id к ModelSpec) — обновить тест data fixture, не менять семантику.
- **Single-shot fallback**: ровно 1 попытка fallback при failure. Recursion через API-модели с `fallback_model_id=None` естественно ограничена. НЕ добавлять explicit `_fallback_depth` counter — модель-каталог гарантирует единичный уровень.
- **Fallback-triggering codes fixed**: {AUTH_FAILED, PROVIDER_UNAVAILABLE, TIMEOUT}. Не расширять без explicit user ask. Особенно **не** трогать RATE_LIMITED (concurrency gate) и MODEL_MISMATCH (семантическая проблема).
- mypy через `.venv/Scripts/python.exe -m mypy src` (не глобальный python — цепляет unrelated `middleware.py:120` на Py3.13). ruff чистый.
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: этот спек не будет переписан во время выполнения (lesson from batch-87 race).

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-89-{task-id}-report.md` + `.result.json` (Auto-Accept). Отчёт CORE-model-fallback-policy должен явно зафиксировать: (a) какие модели получили fallback_model_id=не-None, (b) какие test id в test_router_fallback.py, (c) подтверждение что existing router тесты всё ещё зелёные (perf-side effect).
