# Batch 94 — Browser coverage blind spot (audit P1 #2)
Date: 2026-04-23
Tier: S (2 файла, 0 live quota)

Context (CX read-first):
- Audit 2026-04-23 (`docs/audits/2026-04-23-post-phase-2-audit.md:146-149`): `pyproject.toml:85-88` omits `src/gracekelly/adapters/browser/playwright_driver.py` from coverage. Baseline `97%` coverage headline **не отражает** реальное покрытие этого модуля. Targeted coverage run показывает файл на `65%`, а конкретно `_find_option_in_menu_scope` (scoped menu helper, landed в batch-87) — только `5/7` lines (`71.4%`), exception-path `712-713` (`except: continue` ветка) unhit.
- Риск: playwright_driver.py — hottest + самый failure-prone модуль в проекте. Coverage invisibility означает регрессии здесь проходят незамеченными.
- Reference — fix straightforward:
  1. Убрать omit из pyproject.toml;
  2. Добавить targeted test для exception-branch `_find_option_in_menu_scope` (lines 712-713);
  3. Убедиться что global coverage не упал ниже 95% (если упадёт — это actionable finding, не regression).
- Baseline: HEAD `e3c1b72`, 2650 passed / 97% coverage. ruff + mypy --strict clean.
- 0 live Perplexity submits.

Goal батча: `playwright_driver.py` видим в default coverage report; `_find_option_in_menu_scope` has ≥95% coverage включая exception path; baseline pipeline продолжает работать.

---

## Task: BROWSER-unhide-coverage-and-cover-exception

goal: Удалён omit в pyproject.toml для playwright_driver; добавлен test для exception ветки `_find_option_in_menu_scope`; coverage reports show модуль.
scope: `pyproject.toml`, `tests/test_playwright_driver.py` — 2 файла.
done_when:
- **pyproject.toml** (`[tool.coverage.run]` at line 85-88):
  - Строка `"src/gracekelly/adapters/browser/playwright_driver.py",` удалена из `omit`;
  - Если `omit = []` становится пустым — удалить всю `omit` настройку, либо оставить пустой array если того требует convention в файле. CX выбирает cleanest вариант.
- **Exception-path test** (`tests/test_playwright_driver.py`):
  - Новый тест `test_find_option_in_menu_scope_skips_selector_on_locator_exception`:
    - Mock page.locator(selector_1) raises Exception при .locator("text=X") chain (симулирует Playwright runtime error в scope lookup);
    - Mock page.locator(selector_2) возвращает visible candidate normally;
    - Helper `_find_option_in_menu_scope` должен перейти к следующему selector (исполнить `continue` в except branch, line 712-713), найти в scope_2, вернуть его;
    - Assertion: result из selector_2 (не None, не из selector_1);
  - Если нужно — ещё один тест `test_find_option_in_menu_scope_returns_none_when_all_selectors_raise`: все `model_menu_candidates` raise → helper возвращает None (полное exception exhaustion);
  - Both tests используют тот же mock-стиль что existing tests в этом файле (class TestFindOptionInMenuScope или аналог — реюзать fixtures);
- R1 pass ≥ 2650 + 2 = 2652 full suite; existing tests unchanged;
- `ruff check src tests` clean; `.venv/Scripts/python.exe -m mypy src --strict` clean;
- Default `.venv/Scripts/python.exe -m pytest --cov` (или `coverage run -m pytest && coverage report`) показывает:
  - `src/gracekelly/adapters/browser/playwright_driver.py` присутствует в отчёте;
  - `_find_option_in_menu_scope` ≥ 95% (7/7 или 6/7 lines — CX фиксирует точно в report);
  - Overall coverage не упал более чем на 2pp от baseline 97% (expected drop 1-2pp т.к. playwright_driver вернулся в числитель/знаменатель, и там legitimately unhit paths вне hot code);
  - Если overall упал >2pp — это либо (a) legitimate finding для follow-up batch-95/96 (отметить в report, не блокер), либо (b) regression в тестах (расследовать перед commit'ом);
- Zero изменений в `src/gracekelly/adapters/browser/playwright_driver.py` (production код нетронут), routes, UI, other tests.
visual_check: false
blocked_by: []

Notes:
- Coverage drop после unhide — **не** фейл этого батча. Audit уже указал что модуль 65% targeted; цель — сделать это видимым, не поднять до 95% overall. Последующее доведение — отдельный batch по решению CC после landing.
- CX фиксирует в report: (a) new coverage overall % vs 97% baseline, (b) playwright_driver.py % с unhide, (c) `_find_option_in_menu_scope` line coverage.
- `exc: Exception` — catchall приемлемо в тесте (mock raise), но в prod helper (`playwright_driver.py:710`) уже catchall `except Exception:` — идентично.

---

## Task: closure-chain-next

goal: workflow artifacts, commits созданы CX; если в inbox ещё есть batch-95/batch-96 spec'и — **touch `.ready`** для продолжения pipeline (не удалять, как обычно).
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-94.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-94.md` есть, inbox не содержит `2026-04-23-batch-94.md`;
- `.workflow/outbox/.done` JSON exit_code=0;
- commits созданы CX: `batch-94: BROWSER-unhide-coverage-and-cover-exception — <phrase>`, `batch-94: closure — refresh .done`;
- **Self-chaining**: после `mv inbox/batch-94.md → done/`, если в `.workflow/inbox/` остаются файлы вида `2026-04-23-batch-*.md` (любой spec помимо только что перемещённого) — `touch .workflow/inbox/.ready` **оставляет** marker (или re-touch'ает если он был удалён). Если inbox пустой (после move только Archive/) — .ready удалён как обычно;
- `git status --short` только `?? CLAUDE.md`;
- `git add -f` для workflow artifacts.
visual_check: false
blocked_by: [BROWSER-unhide-coverage-and-cover-exception]

---

## HARD RULES

- **Scope tight**: 2 файла. pyproject.toml + tests/test_playwright_driver.py.
- **0 production code changes**: playwright_driver.py нетронут.
- **0 live Perplexity submits**.
- **Self-chain `.ready`**: в этом батче, т.к. CX знает что batch-95/96 в inbox готовы — чейним их.
- **Commits создаёт CX** (2 atomic).
- **Coverage drop tolerance**: до 2pp от 97% — acceptable. Больше — расследовать перед commit.
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `git commit -am`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: spec не будет переписан.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-94-*-report.md` + `.result.json`. BROWSER report фиксирует coverage numbers (before/after), `_find_option_in_menu_scope` line coverage, test names.
