# Batch 99 — Roadmap relabel Phase 13/14 + gate 2 final sync (docs only)
Date: 2026-04-23
Tier: S (2 docs файла, 0 code, 0 live quota)

Context (CX read-first):
- Plan ref: `docs/plans/2026-04-23-final-closure.md` (second batch of 98→99→100 chain).
- Две секции в `docs/phased-roadmap.md` всё ещё содержат `Remaining` items после batch-97:
  - **Phase 13 Remaining** (line ~224, 5 items): async adapters / Redis rate limit / OpenTelemetry / Sentry / load testing;
  - **Phase 14 Remaining (deferred)** (line ~252, 1 item): async adapters (duplicate Phase 13 entry);
- Все items — **production/multi-user scope**, не активируются для single-user local deploy. По паттерну batch-97 (API hedge `Next:` → `Trigger-reactive follow-up (not scheduled):`) — relabel в тот же формат вместо оставления как "Remaining".
- Параллельно — Gate 2 self-review doc после batch-98 fix'а criterion 2 должен быть unconditional PASS. Но если batch-98 doc update не финализировал все секции — batch-99 добавляет последний sweep (last-updated header, cross-reference consistency).
- Baseline: HEAD после batch-98. `.ready` присутствует (self-chained).

Goal: ни одного `Remaining` / `Open` item в roadmap кроме explicitly labeled `Trigger-reactive follow-up`. Gate 2 doc в final state. Formal roadmap closure state reached.

---

## Task: DOCS-phase-13-14-relabel

goal: Phase 13 + Phase 14 Remaining секции переделаны в `Trigger-reactive follow-up` format с per-item rationale; всё additive (не удаляем, relabel).
scope: `docs/phased-roadmap.md` — 1 файл.
done_when:
- **Phase 13 Remaining** (line ~224): header changed from `Remaining:` на `Trigger-reactive follow-up (production/multi-user scope):` с preamble 1 строкой: `These items are not open work for the single-user local deploy. Each activates on its own trigger:`.
  Items remain, но каждому добавлен rationale suffix:
  - `- Async adapters (async httpx / async playwright) — **trigger**: sustained event-loop stalls under concurrent load. Current sync adapters wrapped in \`asyncio.to_thread\` are sufficient for single-user throughput.`;
  - `- Redis-backed rate limiting for multi-process deployments — **trigger**: horizontal scale-out beyond one uvicorn worker. In-process limiting is sufficient single-process.`;
  - `- OpenTelemetry distributed tracing — **trigger**: multi-service topology или collecting perf data across requests. Single-user local has structured logs.`;
  - `- Error tracking integration (Sentry) — **trigger**: production fleet where operator not the user. Single-user local reads logs directly.`;
  - `- Load testing framework — **trigger**: SLA-bound deploy or before a material scale change. Not required for local scope.`;
- **Phase 14 Remaining (deferred)** (line ~252): header changed from `Remaining (deferred):` на `Trigger-reactive follow-up (merged with Phase 13):` + single bullet:
  - `- Async adapters (async httpx.AsyncClient) — merged with Phase 13 async-adapter item above (same underlying work, same trigger). \`asyncio.to_thread\` wrapper remains in place until triggered.`;
- **Header top** (line ~3): update `Last updated: 2026-04-23 (roadmap formally closed; all phases complete, gates PASS, trigger-reactive items explicitly scoped)`.
- existing Phase 0-17 Delivered секции нетронуты;
- Phase 17 Closed/Scoped out (из batch-97) нетронут;
- Parallel track trigger-reactive note (из batch-97) нетронут;
- Commit: `batch-99: DOCS-phase-13-14-relabel — trigger-reactive closure for async/Redis/OTel/Sentry/load testing`.
visual_check: false
blocked_by: []

---

## Task: DOCS-gate-2-final-sync

goal: Gate 2 self-review doc обновлён в consistent final state (если batch-98 уже не полностью это сделал) — unconditional PASS, deviation перенесена в "Fixed follow-ups" subsection.
scope: `docs/gates/2026-04-23-gate-2-operational-review.md` — 1 файл.
done_when:
- Читать файл — если всё уже в final state после batch-98 (`## Verdict` говорит `PASS for single-user local deployment scope` без "with conditions", `## Findings / deviations` не содержит открытых deviation'ов по shallow probe) — коммит skipped с пометкой "already synced in batch-98" в closure report;
- Иначе — последний sweep:
  - Criterion 2 status cell = `PASS`;
  - `## Findings / deviations` → "## Fixed follow-ups (batch-98 <SHA>)" subsection если оригинальная deviation ещё в файле;
  - `## Verdict` без conditions;
  - `## Signature` с "batch-99 sync pass" annotation;
- Commit (если нужен): `batch-99: DOCS-gate-2-final-sync — remove conditions, final PASS`.
visual_check: false
blocked_by: [DOCS-phase-13-14-relabel]

---

## Task: closure-chain-next

goal: workflow closure, self-chain `.ready` т.к. batch-100 ещё pending.
scope: `.workflow/outbox/.done`, `.workflow/done/2026-04-23-batch-99.md`, `.workflow/inbox/.ready`
done_when:
- `.workflow/done/2026-04-23-batch-99.md` есть;
- commits созданы CX: `batch-99: DOCS-phase-13-14-relabel — <phrase>`, optional `batch-99: DOCS-gate-2-final-sync — <phrase>` (если потребовалось), `batch-99: closure — refresh .done`;
- `.ready` **остаётся** (re-touch) — batch-100 pending;
- `.done` exit_code=0;
- `git status --short` только `?? CLAUDE.md`.
visual_check: false
blocked_by: [DOCS-gate-2-final-sync]

---

## HARD RULES

- **Scope tight**: 2 docs файла.
- **Additive only**: relabel и обогащаем — не удаляем existing content (кроме конкретных "with conditions" phrases которые batch-98 уже мог обработать).
- **Self-chain**: `.ready` остаётся для batch-100.
- **Commits создаёт CX**.
- CLAUDE.md не трогать.
- Запрещено: `git add -A`, `--amend`, `reset --hard`, `push`, `skip`, `xfail`.
- Spec stability: не будет переписан.

---

## Отчёты

`.workflow/outbox/2026-04-23-batch-99-*-report.md` + `.result.json`. Reports: line numbers changed, rationale per trigger-reactive item.
